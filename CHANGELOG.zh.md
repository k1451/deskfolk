# 更新日志 (Changelog)

本文件记录面向用户的变更。项目当前为 **WIP**，尚未发布稳定版本；`Unreleased` 不代表已发布。

[English version (英文版)](CHANGELOG.md)

## Unreleased

- 手机终端不再动不动就打出「输出过快，已丢弃 N 字节」。以前只要 50 毫秒里的输出超过 8 KiB，多出来的就会被扔掉。Claude Code 这类全屏程序每重画几次、每次手指滑动都会碰到，扔掉的字节还会把它的画面弄花。现在多出来的输出排队，稍晚完整送到；只有持续一秒多都比网络快的输出，才会丢掉最早的部分。打开或切换终端时，不再先把整段历史重发一遍才出实时输出。快速切换会话时，也不会再冒出一条大小等于整段输出的丢弃提示。

- shell 命令输出量极大时，Bot 不再以「这一轮没写完：运行时出错」收尾。在工作区里全局搜索、扫进 `tool-results/` 时，一次能打出几百 MB；超过约 2.7 亿个字符后，结果的预览乃至结果本身都建不出来，这一轮就停了。现在 Bot 照样拿到退出码和一小段预览，附一句让它把命令收窄；大到存不下的结果会直说存不下，不再指向一个文件。已存下的大结果附带的提示也请 Bot 在工作区搜索时绕开 `tool-results/`。

- 手机终端里，手指上下滑动现在能翻看 Claude Code、vim、less 这类全屏程序的输出历史了，以前在这些程序里滑动没有任何反应。手指每划过一行，就相当于 Mac 上滚轮或触控板滚动一格。普通 shell 的回滚历史照旧跟着手指滚动。

- 消息里写到的工作区图片路径，现在直接在正文里画成缩略图，和附件、Markdown 预览里的图片一样，点一下放大。缩略图旁写的是文件名，悬停看完整路径。图片滚到附近才加载，取的是小图，排在你主动打开的内容后面；文件已经不在的路径仍是普通链接。Bot 的回复和你自己的消息都这样；还在输出的回复先显示链接，写完再变成缩略图。

- 手机上的终端页进了地址，是 `?o=terminal`，和日程图、花费一样。打开它会多一步，返回键、页头的返回和 Esc 都回到打开它之前的会话或列表。刷新、前进和带这个地址的链接都会再打开终端页。关掉页面不会停掉里面的 shell。宽窗口仍是终端标签，标签不进地址。

- 手机上从群组设置点进某个 Bot 后，按返回又能一层层退了：在技能、日程或记忆的编辑页按返回先回到那一节，再回到这个 Bot 的分节列表，最后才回到对话。以前一按返回就直接回到对话。

- 置顶「文件」会话后，它只出现在置顶一栏，不再同时留在会话列表里自己那一节。置顶格和头像窄栏里仍是文件图标；取消置顶后那一节回来。

- 手机上全屏看图时，按返回会关掉图片，不再退出图片下面那一页。从会话或文件预览里放大的图片会像点 ✕ 一样缩回原处，再按一次返回才照常后退。

- 手机上 Bot 和群组的设置，每一屏都写明是谁的设置。设置页顶栏显示头像和名字（Bot↔Bot 会话显示两个 Bot），从群组点进的 Bot 也一样，以前那里只有一个返回箭头。基础信息、技能、日程、记忆、操作这些分节，群组的成员、会话操作、危险区域，以及技能、日程、记忆的编辑页，都在标题下面多一行小头像和名字。宽屏不变。

- 手机终端 ⋯ 菜单里的「结束会话」又能用了。以前一点它，菜单就在确认出来之前关掉，会话照旧跑着；现在会先问一句，确认后结束这条会话。

- 手机上从会话返回列表时，会话页滑出整块屏幕。「选择一个会话」那一页不再在滑出的过程中闪一下。

- 搜索统一为全局弹窗：展开侧栏的搜索按钮、头像窄栏新增的搜索图标以及 ⌘K / Ctrl+K 都能打开。结果可按 Bot、会话、消息、文件、日程筛选，用方向键选择、Enter 打开；手机上是适配安全区与软键盘的全屏搜索页，顶部有取消按钮。在编辑器和终端里可用 ⌘⇧K / Ctrl+Shift+K 打开搜索，原有 K 组合键继续可用。搜索中、失败重试、离线和无结果分别提示，清空或关闭后会忽略迟到的结果。窄栏的每个头像及功能按钮都增加悬停和键盘聚焦提示，带名称、快捷键、未读或待处理数量，以及更新和工作区未配置说明。

- 终端右边那条白色滚动条去掉了。滚轮和触控板滚动照旧。

- 终端窗格现在可以收到 200 像素宽。原先的下限是 600，大约八十列，和别的窗格并排时终端会占掉这一行的大部分。

- Bot 设置的「日程」页不再套一层带标题的卡片：日程数量和技能、记忆一样显示在标签上，日程直接列在页面里。每行左边是时间、标题和重复方式（每天、工作日、周末或具体星期，不再把时间重复一遍），右边是启停开关和删除按钮；原来的「已启用 / 暂停 / 删除日程」文字按钮让出了位置，标题在窄抽屉里也能完整显示。列表末尾是虚线的「新建日程」；编辑一条时表单直接展开在那一行下面，新建的表单出现在列表末尾。时区和运行条件的说明收成列表下方的一段注释。没有日程时，桌面端也显示空状态和新建按钮。手机上的分区列表同样带上数量，行里的重复方式也不再重复时间，其余不变。

- HTML 文件的渲染预览改成装在一台缩小的电脑、平板或手机里。预览底部的浮条在电脑（1440 × 900）、平板（820 × 1180）、手机（390 × 844）之间切换，整台设备连边框等比缩进预览区、不会放大过真实尺寸，浮条上写着视口和当前比例。设备里的页面和在那台设备上一样：电脑按 1440 宽显示桌面版；平板和手机按移动浏览器的方式排版，用页面 `<meta name="viewport">` 要的宽度，所以响应式页面显示的是它的移动版；没声明视口的页面按 980 宽（内容更宽就按内容宽，1080 宽的海报能整张看到）排版再缩到屏幕宽，和手机打开时一样。每种设备里都能用滚轮或触控板滚动，页面用 `overflow: hidden` 锁了滚动、内容却比屏幕高时也能滚（只放开纵向）；平板和手机里没有占宽度的滚动条。换设备会重新加载页面，让它按新尺寸从头初始化；批注模式里也能换，下次打开 HTML 预览用的还是上次选的设备。浮条末尾的按钮让整块预览（连批注条）铺满窗口，设备可以放大过真实尺寸；页面不重新加载、滚动位置不变，按 Esc 或再点一次按钮回到窗格，焦点在页面里时 Esc 也管用。全屏时点选了要批注的元素，也会回到窗格写批注。窄窗格里浮条只留图标。

- Bot 在最终回复里按 shell 的视角写的文件名（刚在 `work/<任务>/` 里生成的 `poster.png`）现在点得开了。shell 在任务的工作目录里跑，会话里的链接却按工作区根目录找，所以点了就提示找不到文件。回复入库时现在会改成能找到的路径，和任务中途发的消息一样；工作区根目录下确有同名文件、或 Bot 还没生成的文件，保持原样。之前已经存下的回复，显示时按这条消息交付的附件来找，恰好对上一个时就指向它。

- 会话列表收成头像窄栏之后，底部不再只剩设置：列表底栏的「工作区 / 工具 / 设置」原样搬过来，变成三个图标，顺序相同，名字在悬停提示里。工作区没配置时和列表里一样是灰的并说明原因。工具菜单和列表里的是同一个（日程、花费、新建终端，分隔线后是已归档会话，带数量），从窄栏旁边弹出、底边和按钮对齐，右边放不下时改到左边；↑ ↓ 打开并落在首尾项，Esc 关掉并把焦点还给按钮，点别处也关。已归档会话只在完整列表里才有，从窄栏点它会展开列表并直接停在已归档那一页。

- 重新设计产物预览的批注面板与顶栏：批注卡片加入与画面标记一一对应的序号徽标，精简图片选区等技术坐标展示并在悬停提示中保留完整参数；状态徽标与操作按钮（标为已处理、重新打开、编辑、删除）更清晰紧凑；筛选栏改用分段控制风格；顶栏「显示已处理」与批注模式操作视觉统一。

- Bot 回复完成后的耗时改为时间旁的低对比度常规字重文字，去掉彩色背景和描边。

- 批注一被处理掉就从文件上撤下。Bot 在任务里把一条标成已处理，或你在列表里点「标为已处理」，图上的框、代码页边的点、Markdown 里的标记、PDF 页上的框、HTML 的编号和音视频时间轴上的标记当场消失，留在文件上的就是还没处理的。批注列表里它们照旧列着，按「已处理」也筛得到。预览栏多了一个「显示已处理」勾选框，只在这个文件有已处理的批注时出现，勾上把它们画回来，只对这块预览有效。从列表点一条已处理的去定位，那一条在定位期间画出来，再点一下选中的那条就取消选中、撤下来；在对话里点它的卡片只打开列表停在那一条，不画回文件上。

- 获取端点的模型列表时，不再把“慢”说成“连不上”。一次请求 12 秒内没有回应，会再试一次；再试还是没回应，Mac 上的报错写明端点没有及时回应、试了几次，而不是 “Failed to connect”。促成这次修改的那回，连接其实是通的，只是中转站迟迟没回；丢包、根本连不上的主机同样一声不吭，所以也按这样重试和报错，前后约 24 秒。时间到了列表还没传完的，也算没回应，不再说成“没有找到模型”。连接被拒、端点返回 HTTP 错误的，照旧立刻报错，不重试。配对设备发起的探测，每次请求前（重试那次也算）都会重新核对这台设备的权限；手机上探测失败仍只显示统一的拒绝提示。

- 桌面端窗口分成多块窗格后，在侧栏点一条会话不再替换窗格正看着的内容。只有一块窗格时照旧：它替换这块窗格正在看的那条会话。有多块时，已经开着这条会话的窗格切到前面；哪块都没开，就在当前窗格里新开一个标签，摆在你原来放的那些旁边。浮动窗格也算分了屏。

- 桌面端左侧的会话列表可以收成一条只有头像的窄栏。点搜索框右边的按钮，或在任何地方按 ⌘B（输入框里也行）收起，窗格占去其余的宽度。窄栏里还是列表上的那些会话，顺序和分组都一样（置顶、文件、群、你↔Bot、Bot↔Bot），Bot 的状态点照旧，头像角上的数字也一样：先显示待审批，没有才显示未读。点头像打开会话，右键是它的菜单，名字在悬停提示里。点窄栏顶上的按钮或再按 ⌘B 展开，宽度还是你拖过的那个；设置留在窄栏底部。收起与否重启后保留。手机和 680 像素及以下的窗口不受影响。

- 重新设计花费页：窗格顶栏常驻时间与筛选，概览和调用明细分开，总用量与金额层级更清晰，趋势按可用宽度展示并可点选查看，可展开用量说明。桌面分屏、浮动小窗及手机采用纵向记录、日期和排序选择、明确的消息跳转；调整大小保留筛选、当前视图和已载入分页。

- 下一步建议改成按发送按钮旁的 ✨ 才起草。每次起草都是一次记进花费的模型调用，以前打开会话、每来一条新消息都会自己去拉，不管你看没看。起草中再按 ✨ 可以停止，芯片出来后再按一下收起；会话里来了新消息，已有的芯片作废收起。有回复正在进行时 ✨ 不可按，因为这时起草的，回复一到就过时了；按了却什么都没起草出来时，会提示几秒，而不是毫无反应。守护进程给起草的时间也从 8 秒放宽到 20 秒：思考型 flash 模型常常超过 8 秒，按了就回空。

- 会话窄到手机宽度（手机、小窗口或窄的工作台窗格）时，输入区不再浮在转录上，而是整条贴底、贴两侧的底栏，转录停在它上方，建议芯片也在这条底栏里。芯片一行仍然横向滚动，但有更多的那一端会渐隐，鼠标滚轮在这一行上也改为横向滚动，窄窗格里被挡在边缘外的芯片不用触控板也能滑到。这时一旦有文字或文件要发，✨ 就让开，把位置还给输入框。在触屏上，底栏离两侧留 16 像素、离底边留 12 像素（四曲面屏的四角向外弯，也不报安全区），按钮放大到 40 像素，附件和 ✨ 分在输入框两侧，✨ 和发送之间隔 8 像素。更宽的会话仍是浮动的输入卡片。

- 桌面窄窗格里，会话的顶栏并进了它的标签。窗格自己窄到 680 像素及以下时，顶栏占着 64 像素的一行，说的是上面标签已经说过的名字。现在标签在名字前面带着头像和状态点，顶栏的第二行（在场的人，或 Bot 的状态和模型）变成标签的提示。悬停标签出现 ⋯，里面是置顶、经过和会话设置；任何会话标签上右键，也会在分割方向前面给出这三项。从不在前面的标签打开设置，会先把它切到前面。宽的窗格照旧显示顶栏；手机不变。

- 桌面窄窗格里，其他类型的标签也在名字前带上了图标，用的是打开它的地方那个图标：终端是命令提示符，工作区是文件夹，日程是日历，花费是 $，会话的流程是流程图，产物是一个包裹。和会话头像一样，窗格窄到 680 像素及以下才显示；名字被截断时，完整名字在标签的提示里。

- 桌面侧栏底部从七个图标收成带文字的「工作区 / 工具 / 设置」。工具菜单依次是日程、花费、新建终端；分隔线后是已归档会话，数量只在菜单里以灰色显示。新建终端新开一条 shell；已经开着的从窗格的「＋」接回。日程和花费再打开时定位到已有标签。工作区仍是 ⌘O，更新提示仍在设置上。外观改到 设置 → 基础偏好 → 外观。手机仍用搜索旁的工具菜单（仍叫终端，打开终端页），底部导航保留。

- 桌面空窗格右上角新增关闭按钮，窗格右键菜单也可「关闭窗格」，适用于分屏与浮动窗格，并关闭其中全部标签。工作区或产物预览有未保存编辑时先确认；终端会话继续运行，可重新接回。窗口收窄时分屏按比例收缩，关闭按钮保持可见。

- 新增独立花费视图，支持时间范围、调用类别小计、按日趋势、可排序的模型 / 会话 / Bot 表、叠加筛选和分页调用明细。桌面打开一个窗格标签，手机打开浮层；新调用实时更新，实报金额和估算金额分开显示。
- 花费账本记录六类调用、实际模型与端点及名称快照；清空会话、删会话和删 Bot 都保留历史花费。旧记录原地迁移，无法回填的模型保留缺失。新增守护进程聚合和分页明细接口，本机与加密远程连接共用，快照省去持续增长的账本。
- 模型属性新增可选的输入、输出和缓存输入计费单价，单位为 USD / 百万 token，与选路参考价并列。设置自动保存，Bot 工具也可修改；缺实报金额的调用按已知 token 和写入时的单价估算，后续改价保留历史估算。

- 批注，第四部分（各种产物）：Markdown 预览里直接划词批注，位置记回源文件的行和列。图片和 SVG 点「批注模式」拖一个框，框里的裁图随批注交给 Bot。PDF 预览改用内置的 pdf.js 渲染——翻页、缩放、适合宽度 / 整页、页内查找、跟随深浅色——批注模式下在页面上框一块，带页码、框里的文字和裁图。HTML 在批注模式下点选页面上的元素（↑ ↓ 换外层或内层，Enter 选定，Esc 退出），交给 Bot 的是元素的 CSS 路径、文字和 HTML 片段；页面仍在原来的沙箱里，选取脚本只在批注模式下注入，脚本被所在环境的 CSP 拦下时会提示改到「源码」按行批注。音频和视频在当前播放位置打一个点，或「到此为止」延长成一段，时间轴上标着已有的批注。在远控手机上，裁图会先压到一帧装得下的大小再发，压不下就不带裁图照样存。批注模式里按 Esc：有待写的先取消，没有就退出批注模式，再按才关面板。

- 批注，第三部分（信使：框架与文本）：预览里的「批注」按钮打开这份文件的批注列表，可按全部 / 待处理 / 已处理 / 草稿筛选，点一条定位回原处，草稿能改能删，发出去的能手动标成已处理或重新打开。代码和文本（包括 Markdown、HTML 的源码视图）选中文字后点「批注」，手机上点行号选中一行；行号栏的点和高亮标出每一条。预览底部的发送栏攒着这个对话的草稿，补一句总话一起发出；Bot↔Bot 私聊里的产物会写明发到你和哪个 Bot 的私聊。编辑器里有没保存的修改时不能加批注（从源码切到渲染再切回来也算）；选好位置后文件被改过，存草稿时会拦下并说明。一次最多发 50 条，多了先发最早的 50 条。聊天里那条批次消息下面是一张张批注卡片，点开跳到文件的那一处，Bot 处理完卡片当场变成「已处理」并带上它的说明。预览面板窄的时候，批注列表放到文件下方。

- 批注，第二部分（Bot 侧）：带批注的消息会在正文后面把每条批注展开给 Bot——路径、位置、引文或作为图像的裁图、你的意见——原文已变、文件不在了的都标出来。Bot 多了 `list_annotations` 查这件事里还没处理完的批注，`resolve_annotation` 写一句怎么改的把它标掉；读到还有待处理批注的文件会被提醒。轮次指令要求 Bot 逐条处理，不同意就说明原因，不许默默跳过。群里一批交给几个 Bot 时，每条都写明交给谁，每个 Bot 只改交给自己的、在自己交付的那个工作目录里改。

- 批注，第一部分（数据与接口）：本机现在记得批注——你对 Bot 交出的产物某一处写的一句意见，挂在工作区路径、一个位置和交出它的那条消息上。草稿存在本机，窗口崩了或换台设备都还在。攒一批发出去会合成一条引用交付消息的回复并 @ 交付的 Bot，草稿转为待处理，Bot 在交出产物的那个工作目录里被叫醒——哪怕已经过了几个小时。Bot 处理完写一句怎么改的把它标掉；你也能手动关掉或重新打开。文件改了标「原文已变」或给出新的位置，不藏起来；文件删了标「文件不在了」。Bot↔Bot 私聊里的产物，批注发到你和交付 Bot 的私聊里并带跳回链接。链接不能批注。在预览里写批注的界面在后面几部分。

- 官网演示和文档里有了远程访问。演示多出第十一步：Mac 的窗口仍然藏着，配对过的手机从中继页面打开群聊，给 `@Writer` 追一句，Writer 在 Mac 上开工，手机上看着它跑；手机上读过，Mac 的 Dock 角标也跟着消掉。文档站多了「指南」一组和「远程访问」一页——配对过的设备能做什么、部署中继、让源码态的 Mac 连上、配对手机、Web Push，以及各种出错状态是什么意思——由新增的 `docs/remote-access.md` 与 `docs/remote-access.zh.md` 生成。两份 README 都加了「远程访问」一节，路线图也改正为配对用一次性配对内容而不是扫码。远程访问仍是默认关闭的实验功能，发布包暂时不能配对。

- 修复工作台产物预览里，文件明明在，点开却显示「文件不在了」。在文件树里点过的文件会被当成来源消息交付的附件存进标签，带着一个编出来的附件 id，再点一次就拿这个 id 去读附件，读不到。现在这类文件一律从工作区读；点文件只换当前显示的文件，不再往标签里加东西，所以蓝点只标来源消息真正交付的文件，不会跟着点过的文件越来越多。已经存下的标签打开时自动清理。另外，文件树里不再列出被引用过、但已经从磁盘上删掉的文件——这件事的文件列表、流程图卡片打开的列表和消息自己交付的列表都一样。

- 修复预览里从图片（或 PDF）切到视频、音频时显示「文件不在了」。切过去的一瞬间，新文件的字节还在路上，播放器却先拿到了上一张图片的数据，放不了就报错，报错又一直留着。现在播放器、图片和 PDF 只用属于当前文件的数据。

- 修复会话的产物预览在别的窗格里滚一下或点一下就跳回第 1 行。换到显示另一条会话的窗格，或者来了新消息，都会把同一个文件的代码编辑器拆掉重建，还没保存的修改也跟着丢了。现在只有打开别的文件才会重建编辑器。

- 修复配对过的手机一发文件就断线重连。手机把整个文件一口气塞进连接，中继的限流（Mac 与所有设备共用 2.5 MB/s、突发 256 KiB）在上传途中直接关掉链路，手机重连后 Mac 那边的上传也已取消。现在上传按 900 KB/s 匀速发出，留在 Mac 让给设备的那份额度里，浏览器发送缓冲里最多只压 256 KiB；一段 20 MB 的视频大约 23 秒传完，而不是永远传不上去。发送没成功时，作曲栏里的附件和文字也会留着——以前发送前就清空了，得重新选附件、重新打字；发送途中附件不能再删。

- 发送按钮现在看得出消息还在路上。以前只是变灰，和「没东西可发」一个样子，经中继发送、或者排在一张正在下载的图片后面时，看上去就像卡住了。现在超过四分之一秒还没发完，按钮保持原色、变成转圈；配对过的手机传文件时改成一圈进度，每个附件上写着传了多少（「12.0 MB · 已传 75%」）。一下子就发完的不显示。

## 0.1.0-rc.6 — 2026-09-24

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

- 修复自托管中继的 Caddyfile 起不来。拒绝文件路径查询的那条规则把 CEL 的 `matches` 当成运算符来写，Caddy 编译不过，用 `deploy/remote/Caddyfile` 执行 `caddy run` 会直接退出。现在改成对查询串调用 `.matches(...)`。

- 修复往 Bot↔Bot 私聊发文件时，文件先被写出去才被拒。这类会话只能看、不能参与，但带文件的消息会先把文件暂存进工作区（没设工作区时是应用的收件目录），然后才检查；那个目录不存在时还会报内部错误。现在先拒绝，什么都不写。

- Bot 私聊里，一轮中断或没写完（连不上端点、端点拒绝、运行时出错、卡住）都可以点「继续」接着做。此前私聊没有输入框，「继续」跟着被收起；「这一轮没写完」里也只有连不上端点能续。群聊和你跟 Bot 的私聊同样能续这些没写完的一轮。

- Real Bot 改名为 Deskfolk。应用、窗口、菜单、托盘、系统通知和手机推送、Touch ID 提示、官网和文档都换成新名字，应用装好后是 `Deskfolk.app`。仓库搬到 [Blackman99/deskfolk](https://github.com/Blackman99/deskfolk)，官网搬到 blackman99.github.io/deskfolk：旧的仓库链接会自动跳转，旧的官网地址不会。你 Mac 上的东西都不动：bundle 标识、数据目录、钥匙串里的密钥和通知授权都照旧。改名之前的版本只认旧仓库地址，「设置 → 关于」里打不开也下不了新版，需要到 Releases 页面手动下载一次新的 `.dmg`。开过「登录时启动」的，关掉再开一次，登录项才会换成新名字。新装的向导推荐的工作区是 `~/deskfolk-workspace`，已有的工作区不受影响。

- 手机上 Bot 设置的「技能」与「记忆」重新设计为触控友好的界面。技能与记忆列表在手机上采用大尺寸卡片排版，右侧配有专属的轻触启停开关；点击卡片直接进入全屏滑动编辑页，标题栏带有返回按钮与保存提示，表单划分为归属 Bot、场景描述、正文规则与危险删除区域，底部常驻全宽保存按钮。技能列表顶部展示技能总数与快捷添加入口，空状态提供引导卡片与大尺寸添加按钮；记忆列表和编辑页完整呈现来源会话跳转与演进学习信息，编辑页底部提供独立删除确认。在手机编辑页中按返回或浏览器后退，会先退出编辑页回到列表。桌面布局保持一致。

- 官网的滚动演示按应用现在的样子重做，从 8 步扩到 10 步。交接之后多了几段：Bot 跑的命令在它那一行下面边跑边滚、跑完折成一行；report.md 在会话旁边分出一块窗格打开；这件事的流程图，卡片上写着每轮用的模型和理由；右键分割出一块窗格，开你自己的终端跑 `pnpm dev`；关窗之后的 macOS 横幅和 Dock 角标。窗口本身也跟上了：每块窗格一条标签栏，会话顶栏有「经过」，会话窗格窄了按手机布局排，对话左缘的消息索引，端点名单里标出默认模型。边界表加了「桌面工作台」和「远程访问」（实验、默认关闭）两行；路线图把流程图、窗格工作台、终端、日程和待办提醒列进已有基础，并单列远程访问一节；Open Graph 图、仓库社交预览图和 README 顶图按新窗口重新生成。手机上快速开始那一节不再把页面撑得比屏幕宽。

- 重新设计手机上的模型配置和名单页：端点卡片用系统选择器切换默认模型，名单入口整行可点；名单使用通栏搜索、大尺寸选择行和整页滚动，模型属性单独成页，返回逐层退出。底部持续显示自动保存状态，失败可重试；搜索后的批量选择只作用于筛选结果。清空启用名单、取消默认模型也能正确保存。

- 行内图片缩略图、用户消息气泡里的附件卡片（含多文件文件夹）和全屏图片查看适配亮暗主题，透明图片的底色、加载进度、文件名和操作按钮同步跟随配色。消息和文件预览共用的图片放大层在切换主题时立即更新。

- 手机上的终端重新排版。顶栏只剩返回、终端名和所在目录、新建「＋」、「⋯」：同时开着几个终端时点标题切换（标题旁标着个数），停止、查找、清屏、字号和结束会话收进「⋯」，结束会话仍要再确认一次。底部换成两排、每排七个的按键：Esc、^C、^D、^V（Claude Code 里用来贴图片）、粘贴、↑、回车，Ctrl、Tab、⇧Tab、键盘开关、←、↓、→。Ctrl 按一下只作用于下一个键，软键盘上敲的字母也算，所以 Ctrl+R、Ctrl+U 这些都能按出来；方向键按住连发，并跟着程序切换的光标键模式走；「粘贴」把手机剪贴板里的文字当作粘贴送进去。点这些键不再把焦点交给终端，键盘收着就还收着，开着就还开着；要打字点终端，或者点键盘开关。软键盘弹起时整页缩到键盘上方，按键贴着键盘，顶栏不再被 iOS 推出屏幕。桌面窗格里的终端不变。

- 手机上从群组点进某个 Bot，打开的就是这个 Bot 的设置页。顶栏是一枚返回图标，点它回到对话，不再回到群组设置。较宽的窗口仍可从这里退回群组设置。

- 修复 Dock 上应用图标的角标消不掉。一轮被中断或没写完，这一项要等你点「继续」才算处理完；以前你看过了也照样算在角标里，而信使没有通知页，没有别处能把它划掉——没打算继续那一轮，角标就一直挂着，往那条会话里再发消息，行上的「中断」没了，数字还在。现在角标只算没看过的，和还在等你批准或回答的。中断和未完成看过就不再计入，会话行照样标着，「继续」照样能点。通知中心里那条会话的横幅也按同一个口径撤下。

- 远程视频和音频预览支持边加载边播放，拖动进度按需读取对应片段。消息附件、工作区和流程图产物共用这条播放方式；关闭预览会取消未完成的读取，媒体继续通过加密连接传输，浏览器不持久缓存媒体内容。

- 修复桌面上工作区窗格的文件树点了没反应。点下的文件只记到了窄屏那层浮层上，窗格里的工作区标签看的是它自己记的那个文件，所以预览一直不动。现在点一个文件，这块窗格就打开它，标签名跟着换；退出再开也还在这个文件上。窄屏和手机上的工作区不受影响。

- 手机推送通知补上 Real Bot 应用图标，与已安装的 PWA 使用同一张图；Android 通知栏同时使用透明底单色小图标。

- 手机上编辑 Bot 日程改成整页。点一条日程进入编辑：时间用系统时间选择，星期是一排可点的日子，启用是开关；删除在页面底部。返回先回到日程列表。宽屏仍在原来的卡片里编辑。日程列表的条数和「新建日程」在页面顶栏里，提示和日程行与页边对齐；编辑页的顶栏和列表页一样，进入时不自动聚焦输入框，键盘不会一进来就弹出。「每天 / 每周」切换里选中的一格填满外框，上下不再比左右空出一截；启用开关在行里上下居中。

- 会话窗格比手机还窄时，改用手机的布局。以前手机布局只看整个窗口的宽度，工作台里一块 380 像素宽的会话窗格仍是桌面排法：顶栏三个带字的按钮把会话名挤到一边，群里 Bot 的名字和模型名在消息上方一个字一个字地折行。现在看的是会话自己的宽度：窄于 680 像素时，顶栏的操作收进「⋯」，消息上方名字截断、模型名交给顶栏，消息索引和「Enter 发送」提示收起，输入框换成手机上的紧凑样式。用鼠标时悬停按钮条照旧；窗格里也不出现返回按钮，标签栏就在上面。手机上的样子不变。

- 修复窗口较窄时，消息里的文件卡片伸出气泡、压到头像底下。卡片按文件名的长度定宽，最宽 240 像素，不管气泡有多宽；截图这类名字长的文件，气泡一窄就放不下。现在卡片跟着气泡收窄，文件名和路径末尾省略。

- 修复短消息上方的悬浮按钮点「复制」后，「✓ 已复制」一个字一行地竖着折开。按钮条是按气泡宽度排的，「你好」这样的一两个字的气泡装不下它。现在它始终排成一行；比气泡宽时朝远离头像的一侧伸出，不再压住头像。

- 终端的屏幕改由守护进程持有。守护进程给每条终端会话留着一块屏幕，和 shell 同步解析每一个字节；接回一条终端时，窗格拿到的是这块屏幕现在的样子，再从那一刻接着往下收，不再把最近 256 KB 的原始字节重新放一遍。vim、htop、Claude Code 这类全屏程序接回来就是原样，光标、配色、鼠标和滚动区域都在，而不是一片错位；手机上看桌面开的终端也一样。程序问终端的问题（你是什么终端、光标在哪、背景是什么颜色）也改由守护进程这一处回答：桌面和手机同时开着同一条终端时，不会再答两遍、把多出来的那份当成你敲的字。颜色问题按最近接上的那个窗格的配色回答，所以在浅色下，neovim 这类会问背景色的程序会自己换成浅色。⌘K 清的也是守护进程那块屏幕，换个标签或在手机上接回，旧的内容不会再回来。退出再开时，记下的是这块屏幕本身，而不是一段原始字节。

- 修复手机通知在旧推送重试过期后卡住：再次测试会报 `remote request rejected`，自动通知也可能一直发不出去。Mac 会在恢复和发送前结束过期投递；已有推送仍在发送或重试时给出明确的等待提示。测试反馈保留超时诊断，并按当前语言说明等待、限流和配置缺失的原因。

- 手机上在文件预览里换一个文件，不再多出一步返回。以前每换一个文件都记一条历史，返回要把看过的文件倒着走一遍才离开预览；现在返回直接回到打开预览之前的页面。工作区里换文件也一样，返回回到工作区。

- 设置里的模型端点拆开了。每个端点上，已启用的模型各占一行，点那一行就把这个名字设成默认模型。连接（名称、URL、密钥）和名单（启用哪些、价格、思考等级、擅长领域）各自单独打开。以前默认模型藏在一张长表单底部的菜单里，打不开。

- 应用里的终端用起来更像 Mac 上的终端。画面改用 GPU 绘制（不支持时退回原来的画法），大段输出和全屏程序不再发卡；emoji 和宽字符按两格算，TUI 的边框不再错位。颜色跟随应用的浅色 / 深色，16 色换成和应用同一套；浅色下过淡的颜色（黄、「白」）自动加深到看得清。此前终端无论浅色深色都是黑底，因为它引用的主题变量根本不存在。Mac 的编辑键补齐了：⌘← / ⌘→ 到行首行尾，⌘⌫ 删到行首，⌥⌦ 删掉后一个词（⌥← / ⌥→、⌥⌫ 本来就能用）。⌘K 清屏；⌘F 在终端里查找，回车下一个、⇧回车上一个，⌘G / ⇧⌘G 也行，Esc 关掉；⌘+ / ⌘− / ⌘0 调字号，所有终端共用一个字号，记在这台机器上。网址按住 ⌘ 点击打开，程序用 OSC 8 标出的链接也一样；手机上点一下就开。在接管鼠标的程序里（vim、htop），按住 ⌥ 拖动仍能选中文字。右键终端时，菜单最上面是「复制」「粘贴」，右键会先选中光标下的词；粘贴直接读系统剪贴板，不用再多点一次确认。

- 修复新开的终端一直以为自己是 80×24。会话先按 80×24 开出来，窗格量出实际大小后却没告诉 shell，要等你拖一下窗格才对：在那之前长行在 80 列就折行，vim、htop 这类全屏程序只画在左上角一块。现在终端一显示出来就按窗格的大小来。

- 修复接回终端时提示符里凭空多出 `1;2c` 这样的字符，以及退出再开后提示符上面一行行叠起来、夹着反白的 `%`。接回一条终端（切标签、重新载入、重启后连上）时，屏幕是把这条会话的历史字节重放一遍画出来的；历史里的程序当年问过终端的话（比如 Claude Code 启动时问的「你是什么终端」），xterm 会再回答一遍，答案被当成你敲的字送进了眼前的 shell，接回一次多一段。现在重放历史时不再回答这些问题，程序实时问的照常回答。退出再开时，旧屏幕和新 shell 之间原来什么都没隔开：新提示符接着旧的半行往下写，zsh 就标一个反白的 `%`；而收尾时「退出全屏」又会把光标带回提示符自己存过的位置，每重开一次就叠一行。现在旧屏幕先被收好——颜色、字符集、滚动区域、鼠标和焦点报告都复位，只有真停在全屏程序里才退出全屏——新 shell 从新的一行开始。已经留在历史里的那些行不会消失；想要干净的屏幕，结束这条会话再开一条。

- 终端里的 shell 拿到的环境和 Terminal.app 一样齐。此前只有 `PATH`、`HOME`、`SHELL`、`TERM`、`LANG` 五个：ssh-agent 里的密钥用不上（没有 `SSH_AUTH_SOCK`），`USER`、`TMPDIR` 是空的，neovim、bat 这类工具只按 256 色画。现在补上 `SSH_AUTH_SOCK`、`USER`、`LOGNAME`、`TMPDIR`，设了 `COLORTERM=truecolor` 和 `TERM_PROGRAM=RealBot`；守护进程自己没有 `LANG` 时，按系统的地区设置推一个，和 Terminal.app 一样。守护进程的其他环境变量——包括各种密钥——仍然一个都不带进去。在 zsh 里，`cd` 之后终端的标签名和路径跟着变，退出再开也回到最后所在的目录：守护进程给 zsh 加了一小段启动脚本，每个提示符前报一次当前目录（OSC 7，和 Terminal.app 的做法相同），你自己的 `.zshenv`、`.zshrc` 照常加载。bash、fish 暂时还停在开的那个目录。桌面和手机同时开着一条终端时，行列按你最近在看、在敲的那一端来：手机看过之后，回到桌面敲一个键就恢复成桌面的大小。

- 修复远程连接下放大图片会变两次大小。之前先放大成一个带转圈的大框，图片到了之后又变成图片自己的比例。现在直接从屏幕上已有的缩略图开始（没有时先取 15 KB 的缩略图），一次放大到图片的实际大小；清晰版到达前，进度显示在缩略图底部，到了之后原地替换。什么都还没有时，框停在原来的位置，进度显示在屏幕中央。「查看原图」的位置从一开始就留好，按钮出现时图片不再移动。

- 远程连接要下载的东西大幅减少。手机回到应用时（息屏后、换网络、出隧道），现在只向 Mac 要这段时间里发生的变化，通常几 KB，当前对话保持不动；以前每次都要重下全部状态（真实数据下 1.6 MB）和所有打开过的对话。全部状态本身也从 1.6 MB 降到约 150 KB：界面上不显示的用量记录不再放进去，Bot 头像改为发 128 像素的小图（保存资料时不会用小图覆盖原图）。打开对话先载入最近 20 条，往上滚再载入更早的。标记已读不再把整个对话发回来。聊天和笔记里的图片滚到附近才加载，不再一次全部拉下来；笔记里的图只取小图。Mac 发给手机的 JSON 会先压缩（一个对话约为原来的四分之一），文件内容从不压缩。中继对应用的脚本和样式启用了压缩，应用本身也变小了：代码编辑器的样式，以及设置、日历、流程图、文件预览这些面板，改为第一次打开时才加载；消息格式的安全过滤换成了体积小得多的库。首次打开的传输量从约 2.2 MB 降到约 400 KB。手机上缓存的应用文件会清掉一个月没用过的，不再随每次更新不断变大。

- 远程连接下的图片改为先发缩小版，原图点一下就能看。对话里的图片卡片从 Mac 取 256 像素的缩略图，不再拉整张原图；笔记里的图同样只取缩略图；放大图片、预览图片文件时，显示 1600 像素的版本。一张 3.7 MB 的分镜关键帧，卡片里只要 15 KB，放大约 550 KB。屏幕上是缩小版时，放大和预览都会给出「查看原图」和原图大小；点下去原图会原地替换缩小版，进度显示在按钮上。在 Mac 上，卡片同样用缩略图，放大仍然直接打开原图。缩小版由 macOS 的 `sips` 生成并缓存；本来就比缩小版小的图、放大时的 GIF、SVG 都原样发送。

- 修复远程连接下打开文件要在「0 B」停很久才开始加载。手机向 Mac 一次只要一样东西，而当前对话里的每张图都排在前面，要的还是整张原图，连折叠成文件夹、根本不显示的图也在要。图多的群里，一份 5 KB 的笔记前面能排着几十 MB。现在折叠的文件夹不再拉图；对话里的图排在你要的其他东西后面；打开文件或放大图片时，正在下载的图会先暂停，之后从头重下；离开对话会撤掉还没下的图，关掉预览会停掉它的下载。连接断开时，排队中的请求也会结束，不再一直转圈。

- 修复手机上「文件」会话的顶栏。返回、文件图标、标题和标题下那一行现在排在同一行里，那一行放不下时末尾省略，不再穿进图标。

- 修复手机上文件预览的顶栏。文件名在这一条的中间，挨着返回，不再横在返回箭头上面。名字放不下时末尾省略。这一条也让开状态栏。

- 桌面的分屏，连同里面的终端，退出后再开还在。分屏本来就记在这台机器上。还开着的终端会在原来的窗格、原来的目录里重新开一个，屏幕上的内容也还在；结束会话才不再恢复。关窗仍然不停。

- 应用里画出来的图片可以右键复制。消息里的图、放大后的图、文件预览和流程图里的图，以及输入框里待发送的图片，右键都是「复制图片」，复制的是画面上的像素。头像和图标不在这里面。按住 ⌥ 右键仍是网页视图自己的菜单。

- 修复：图多的群里，Bot 一轮反复报「连不上端点」、点继续也不行。转录窗里每张栅格图都会随每次请求原样发出，一个分镜群攒到一轮 54 张、85 MB，端点六分钟内回不了第一个字节，请求超时后被记成连不上。现在一轮最多带 20 张、20 MB 的图，从触发消息起由新到旧取，放不下的旧图只留路径；大图先缩到长边 1568，不透明的转成 JPEG。同一轮变成 8 MB，二十几秒开始回复。

- 远程连接多了一条专门用来发文件的会话。发到这里的文件复制进工作区 `inbox/`，不叫醒任何 Bot。这里也能发文字，留在这条会话里给自己看，同样不叫醒谁，输入 `@` 也不会列出 Bot。桌面的会话列表也有这一条，用来查看远程发来的文件。在普通会话里发的文件仍会叫醒那条会话。

- 桌面工作台里的空窗格，改成和标签栏「＋」菜单同一份列表，直接摆在窗格里：上面是筛选框，下面是新终端、工作区、日程，再往下是可接回的终端（名字下面带路径）。此前是一排换行的胶囊按钮，「接回 · …」一个挨一个。列表上方说明会话从侧栏点进来。

- 群组设置里，群名和头像一起放在面板头部。标题上直接改，离开输入框或按 Enter 就保存。原先的群资料卡片、单独的群名输入框和保存按钮没有了。

- 修复拖边框改窗口大小、松手后窗口跳到别处。记住窗口大小时，拖动的每一步都会问 macOS 这扇窗是不是最大化，这一问会把窗口挪开；现在拖动过程中不问，松手才问。

- 文件预览不再自带一排按钮。桌面端文件名和关闭都在工作台标签上，预览自己的标题、路径、查找、换行、复制、保存、关闭、用系统打开和在 Finder 显示都去掉了。查找仍是 ⌘F，保存仍是 ⌘S。Markdown 和单文件 HTML 在内容顶部正中留一个浮动按钮，滚到哪里都停在那里，在渲染和源码之间切换。文件树里右键一项，可以选择在 Finder 中打开，或用系统默认应用打开；这两项只在桌面窗口里生效。手机上预览只留一条：返回和文件名。

- 修复产物预览编辑器里的查找替换条画到源码上：替换行、替换按钮，以及展开替换的箭头，都留在工具条里面。

- 桌面工作台的每块窗格都能右键，快捷向上、向下、向左、向右分割。在窗格里任意位置右键（会话、终端、预览、标签栏、空窗格都行），选一个方向，就在那一侧新开一块空窗格并成为当前窗格。右键的不是当前窗格时，分的是被右键的那块。窗格里本来就有右键菜单的地方不被盖掉：消息上仍是消息菜单，代码编辑器仍是它自己的菜单；输入框（包括底部的输入栏）仍是网页视图的原生菜单，粘贴在那里。按住 ⌥ 右键，任何地方都是网页视图的原生菜单，「检查元素」也在里面。窗口放不下的方向置灰并说明原因。浮动窗格不能就地分割，它的菜单改为提供「停靠回布局」。⌘\ 与 ⌘⇧\ 仍是向右、向下分割。

- 修复桌面工作台里有一块窗格卡在最小宽度时（比如在终端旁边分割），其余窗格铺不满、最右边空出一条的问题。原先各轨道的比例加起来是 1，一块窗格停在下限后，剩下几块的比例之和小于 1，浏览器就只按这个比例分剩余空间。

- 桌面工作台里，一条会话的流程图和产物预览标签改成固定名称：「<会话名>流程」「<会话名>的产物」。名字跟这条会话自己的标题走（群名，或私聊里对方的名字），流程图换一件事、预览换一个文件，标签都不改。会话已经删掉时，按「已删除」来叫。

- 从远程连接点开消息里的图片时，进度画在图片自己身上。字节还在路上，放大框就先长到该有的大小，里面有转圈、「正在打开文件…」和已经传到多少；缩略图在图片到达之前也转着同一个圈。

- 桌面工作台摆着不止一块窗格时，当前窗格（键盘所在、Stop 打的那块）四周描一圈强调色边框，浮动窗格顺着圆角描。此前只有它的激活标签顶上一条细线，窗格自己的描边被会话顶栏和内容盖住，看不出是哪块。只有一块窗格时不描。

- 桌面工作台里在哪块窗格上滚动鼠标滚轮或触控板，哪块就成为当前窗格，和在里面点一下一样；滚动本身照常进行，不会被打断或跳回底部。滚了别的窗格之后，接着在原来那块的输入框里打字，当前窗格跟着回到打字的那块。HTML、PDF 这类预览在内嵌页面里滚动，窗口收不到滚动，不会切换。

- 文件预览里的图片点一下放大，和消息里的图片一样：从预览里的那张长出来铺满整个应用，点空白、关闭或按 Esc 缩回原处。直接用预览已经读到的那份，不再下载一遍。预览的 Markdown 里引用的图片也是点一下放大，不再把预览换成那张图。桌面工作台、窄屏和手机上都一样。

- 修复桌面工作台里点不在最上面的浮动窗格，第一下点不中：按下去时那块窗格被提到最上面，同时在页面里挪了位置，这一下的点击就丢了，按钮、链接、图片都要点第二下。现在浮动窗格在页面里的位置不变，只改叠放顺序。

- 修复桌面工作台上在会话旁打开的 Bot 设置什么都存不下：选了模型看着生效，关掉再打开又回到「自动」；改名字、职责、边界、头像、思考等级，加技能，归档 / 恢复也都不生效。现在无论从哪里打开，设置都存到它显示的那个 Bot 上。

- 桌面工作台标签栏「＋」打开的菜单改成固定宽度的列表。新终端、工作区、日程留在上面；还在跑、又没在标签里打开的终端列在「可接回」下面，按名字和路径，多了就在菜单里滚动，不再把菜单横向撑开。在菜单里打字，按名字或路径筛这份列表。空窗格里仍是同一批入口，横着排。

- 消息菜单里的「看这件事」打开流程图后，直接移到这条消息自己的那张卡片上，放在视口中间、按能读清的尺寸画。流程图已经开着时，再从一条消息打开，画面从当前位置平滑滑到那张卡片，拖动或缩放会把这次滑动停掉。第一次打开仍是直接落在卡片上。会话顶栏的「经过」仍是整张图适应窗口。

- 消息里的图片不再打开产物预览。点气泡里的图片附件，或正文里的图片链接，会铺满整个应用放大，并从点中的那张小图长出来；收起时缩回原处。点空白、关闭或按 Esc 收起。文件、文件树和工作区里打开的内容仍走原来的预览；消息下面收起多个文件的那张卡片（「work · 4 个文件」）也打开预览，即使里面全是图片。

- 修复桌面工作台里两条会话并排时输入框不隔离：在一个里打字，另一个也跟着出字。草稿、正在回复的那条、待发的附件、建议芯片、发送中的状态、跳转高亮、往前翻的进度现在都跟着各自的会话，在哪块窗格里发送就发到哪条会话，一条在发送时另一条照常能发。点进另一块窗格不再清掉那条会话的回复对象和建议芯片，也不再把其他窗格滚回底部。看得见但没点进去的窗格也会各自标记已读。断线时留下的草稿回到写它的那条会话。手机上切换会话，已选的附件留在原来那条，不再跟到下一条去。

- 修复桌面工作台标签栏「＋」弹出的菜单被下面窗格的内容盖住：会话顶栏、流程图标题都会压在它上面。菜单开着时标签栏整体浮到窗格内容和浮动窗格之上。会话设置的遮罩相应降到浮动窗格之下，浮动窗格不再被它盖住。

- 桌面上一个终端标签就是一个终端：新开终端标签（「＋」、空窗格里的「新终端」）就是新起一个 shell，标签里不再有一排会话和「新建」，只剩这条会话的路径、停止和结束会话。关掉标签后还跑着的会话，以及手机上开的，在「＋」和空窗格里按名字「接回」；侧栏的终端按钮先回到已经开着的终端标签，没有才新起一个。同一个文件夹里开的几条按开的先后编号（「real-bot」「real-bot 2」），桌面标签和手机上叫法一样。手机和窄屏仍把全部终端收在一页里当它的标签。修复终端标签一切换就都变成同一个 shell：标签重新挂载时会去挑最新那条还活着的会话。修复重启后终端标签全部消失：会话列表只在打开终端时才读，读到之前自愈把每个终端标签都当成指着已经不在的会话。

- 拖动分隔条或交汇点时，它两边的窗格都跟着指针变大变小。终端的行列、编辑器、会话的贴底和索引这些测量留到松手再做一次。移动浮动窗格跟着手走，松手才写入记住的布局。

- 议程视图的每一行标出这条日程是哪个 Bot，带头像和名字，和周视图格子上的一样。

- 日程图铺满它所在的窗格。改变窗口或拖动分隔条时，周视图跟着变；时间轴在窗格里滚动，不再是一块被窗格边缘裁掉的固定格子。

- 日程图的名册筛选收成可搜索下拉，不再把 Bot 铺成一排。下拉里按名字或职责找，点一行隐藏或显示这个 Bot 的格子。Bot 的名字始终露在外面，过长的职责在旁边省略。没有日程的 Bot 不出现在筛选里，也不出现在图上。

- 模型选择记录并入流程图，不再有单独的入口和页面：会话顶栏和手机更多菜单里的「模型选择记录」去掉了。流程图上每张 Bot 卡片底下多一行，写着这一轮用的模型、思考等级和消息类别，有模型反馈、复盘归咎模型、补全失败的各有记号；点这一行在卡片下面展开理由、耗时和跳数、每条反馈（点一下跳回原话）、复盘结论和后效、记下的经验。工具条多了「有反馈」「归咎模型」两个开关，把命中的卡片点亮、其余变暗，整件事的形状不变。一件事交接进了别的会话，那边的选择也在同一张图上。代价是不能再在一屏里按会话把所有轮次扫一遍，也没有了按模型 / Bot / 类别的筛选和搜索；本机接口仍可逐会话列出。桌面上的流程图窗格现在也会随这件事的新轮次和消息刷新，此前只有窄屏浮层会。

- 桌面工作台里，会话的 Bot 设置（群里是群组设置）改为在这条会话的窗格里从右侧滑出，下面盖一层遮罩，不再新开一个标签页。遮罩只盖这块窗格，标签栏和别的窗格照常能用；点遮罩或侧栏的 ✕ 收起，从侧栏里拖选文字拖到遮罩上不会误关。群里点某个 Bot，直接就是这个 Bot 的设置，不再带「返回群组设置」。会话设置同一时间只在一条会话上开着，在另一块窗格里打开时原来那处自动收起，没保存的群名跟着它所在的那条会话，不会因为点进另一块窗格就换成别的群的。之前存下的布局里单独的设置或模型选择记录标签会被收掉。顺带修复窗格里会话顶栏的「Bot 设置」按钮和头像点了没有反应。窄屏和手机不变。

- 桌面工作台里每条会话只有一块产物预览、一块流程图。从这条会话里再打开别的文件——附件、文件链接、预览自己的文件树，或流程图卡片上的文件——都是把这条会话的那块预览转过去，不再多出一个预览标签；打开它的另一件事，流程图那一块跟着转过去。只说打开流程图、不指定哪件事，就只是把它带到前面，不换它正看着的那件。预览里有没保存的编辑时，先问保存还是放弃。之前存下的布局里同一条会话有多块预览或流程图的，只留离键盘最近的那块。

- 流程图页（这件事）的标题栏与任务切换选项合并：当前选中的任务名称作为标题本身展示，多任务时支持点击标题展开下拉菜单切换，释放了画板上方的纵向空间并消除了信息冗余。

- 修复桌面工作台中打开消息产物后文件树丢失。预览标签保留来源消息、所属任务与附件列表，恢复布局后仍可浏览文件树；选择文件会在当前预览标签内切换。长文件树在窗格内独立滚动，末尾文件也可正常访问。

- Web Push 支持守护进程的 HTTP/HTTPS 代理环境与 `NO_PROXY`，保留目标校验、固定公网地址、TLS 验证及禁止重定向。远程测试通知分别说明推送服务接受与手机接收状态，首次发送超时时显示原因及已安排重试。

- 桌面系统横幅改为看进程有没有应用标识、系统设置里是否已允许，不再被尚未验收的冷启动点击门挡住。`tauri dev` 会在一个调试用 `.app` 里重新启动，标识仍是 `com.real-bot.desktop`，通知中心才认这个进程，给安装版允许过的权限也直接生效。签名安装包的冷启动点击仍未验收（`NATIVE_DELIVERY_QUALIFIED` 仍为 false），改由 `cold_click_qualified` 单独上报。

- 桌面主栏现在是一块工作台：窗里可以纵横任意分割出多块窗格。一块窗格装一组标签页，一个标签是一条会话（它的产物预览、自己的设置、流程图和模型选择记录都在旁边）、一个终端、日程图或工作区。分隔条可以拖；两条分隔条交汇的地方，那个点横纵两个方向同时可拖，而且两侧的窗格保持在一条线上、不会裂开。标签可以在窗格之间拖，拖到一块窗格的边缘就把它就地分割，按住 ⌥ 拖出来则浮在窗里。排法记在这台机器上，下次打开还是那样；引用的会话或终端没了的标签会自己关掉。命令在「视图」菜单里，⌘\ 和 ⌘⇧\ 分割，⌘W 关掉眼前那个标签页——没有标签页可关时窗口仍然收进托盘。窄屏和手机不变，还是一次一屏、有返回栈。

- 关掉终端标签不再结束会话。标签上的 × 原本直接删掉会话，误点一下就把里面跑着的东西停了，长时间开着的 `pnpm dev` 也在其中。现在它只是把会话从那块窗格拿走、让它继续跑，还能从同一块窗格再打开；要停是另一个会先问你的动作。手机上列的是这台机器的全部会话，那里干脆没有 ×，只有明确的「结束会话」。

- 看一条会话会在它可见的每一块窗格里都标成已读，不只是键盘所在的那块。条件没变：贴着底、窗口真的聚焦、停留一会儿——四条对话摆在眼前，就是四条你看过的对话。

- 修复远控打开一个不大的文件也要等很久。每次下载都拆成独立的加密帧，而且每一帧都要等浏览器报告上一帧已经离开发送缓冲。这份报告比字节本身晚，于是「开始了」之后的那一帧会一直等到超时，链路被断开，预览停在「正在打开文件…」。现在只在发送缓冲真的满了才等；放得进一帧的文件直接跟在响应里。

- 桌面端会话左侧索引改为紧凑短刻度，覆盖已载入的用户、Bot、提问、批准和系统消息。悬停预览显示发送者与内容，点击刻度或预览即可定位；长索引独立滚动，顶部可载入更早历史。

- 「回到底部」浮在输入框右上方。离开底部时从输入框里滑上来，回到底部时按原路缩回去。

- 修复「回到底部」按钮外围一圈方框阴影。圆钮和滑动槽一样宽，外投影会被切成方块；现在只留细边，键盘焦点画在圆内。

- 修复「回到底部」收起时压在输入框上。圆钮从卡片上沿钻到后面；建议芯片仍贴在卡片左肩，并在按钮出现时停在它左侧。

- 修复手机上远程连接断了就一直断着。链路是中继关的、Mac 走了，还是手机换了网，浏览器都会告诉这一页——而这一页没有在听：它接着显示那份对话，像什么都没发生，直到你点一下或发一条，那一下失败了才开始重连。于是重连一直是你触发的。现在这一页自己知道链路没了，立刻按既有退避重连。换网之后那种浏览器也不报的死链——socket 仍称 open，却什么都不再送——在有请求等着回答、发送缓冲不再动、且半分钟内一个帧都没到时，也判成断开：空闲的链路和正在上传的链路都不算。从锁屏、隧道或后台回来时（页面可见、窗口获得焦点、网络恢复、收到推送），下一次尝试会被提前到它本该发生的那一刻，但绝不越过中继每分钟十次握手的额度——越过只会被中继拒到下一分钟，越急回来得越慢。

- 修复没连成的那次握手把设备在中继上的唯一路由占着不放。中继给一台设备一条路由，也只肯给一条，于是握手半路死掉却留着 socket 的那一页，之后每一次重连都被自己丢下的那条链路挡在外面。

- 修复握手用的那个读帧监听器在连上之后继续堆东西。它的活是把没人等的帧先收着，而连上之后就再没有人等了：整个会话的每一个帧都留在那个队列里，一次 50 MiB 下载就是一整份留在手机内存里。

- 消息的文件清单统一通过附件文件树与预览入口查看。新消息省略自动追加的附件路径；旧消息末尾已被附件覆盖的纯路径链接列表会收起，正文中带说明的文件引用继续保留。

- 这件事的流程图卡片在名字旁边带上这个人的头像。点一张被中断的卡片，会定位到那条「中断」本身；以前会落到它上面那一条。

- 重新设计设置中的通知页面，对齐整体卡片与行布局规范：采用仿原生 macOS/iOS 开关组件替换原生复选框，为各通知类别添加视觉图标与清晰说明，优化免打扰时段选择体验，并全面适配移动端单列自适应排版与触控热区。

- 终端面板在最后一行下面留出间距，提示符不再贴着窗口底边。

- Bot 用单独一行 `附件：工作区路径` 交出的文件会收成气泡下的附件卡片，点开即可预览。已经发出去的旧消息同样收成卡片，正文里不再重复列这一行。

- 修复一条交出 21 个文件的消息，点开文件树只有 4 个。那棵树只按这件事的记录来列——记录的是 Mac 看见某一轮写了、或链接了的东西——所以消息用单独一行 `附件：` 交出的文件都不在里面，连正在看的那个都不在；Mac 学会读这种行之前存下的消息，全是这种情况。现在树是这件事的记录，加上这条消息自己交出的东西；而且这份名单和气泡下那张卡片是同一种读法，两边不会再各说各的——正文里的 `1/3` 这种也不会被当成文件列进去。

- 修复聊天旁边那条文件树、以及工作区的文件树，一有风吹草动就把已经列出来的东西丢掉。这两块面板的入参都挂在同一个由快照推出来的对象上，任何一份新快照都会让它们重跑一遍，于是先清空、再把同一份拉回来——看上去就是树在闪。现在只有换了一件事、换了工作区、或者重连，才会重新列。

- 修复看一眼对话就每秒重标一次已读。Mac 用 `session.upsert` 回话，于是回来一份并没有新东西的快照；转录只要见到新快照就重新计时，这一圈就绕不出来了，对话开着多久它就标多久，而且每转一圈，所有挂在快照上的面板都跟着重建。现在一条消息只标一次已读。

- 修复 markdown 里没有语种的代码块每秒重画十来次——消息里、文件预览里都一样。这种块画出来就是它自己，而“已经画过了”是靠找一个高亮的 span 判断的，纯文本永远没有那个 span；每次重写又惊动了那个负责喊下一次重画的观察者。

- 修复桌面端会话容器缺少高度约束导致输入框消失、聊天记录无法滚动的问题，输入框保持可见，长会话在主区域内滚动。

- 手机上点开会话时，对话从右侧滑入并盖住会话列表；返回时向右滑出，底下的列表保持不动。更宽的窗口仍是列表和对话并排。

### 通知（阶段 1–7 候选；原生投递仍未合格）

- 事务性收件箱：语义唯一键、独立阅读与处理状态、精确 `pending_ask_id`，覆盖待批准、提问、失败、中断、面向你的回复与日程结果。
- 有界标读：`message_seq` / `read_through_seq`，`/v1/sessions/:id/read` 可带 `through_message_id`。HTTP 首页/更多页按自身水位应用后再回放一段有界实时通知尾，忙时事件流不会饿死空首页，也不会丢掉之后的已读/删除。尾不完整（invalid / 实例切换 / 超过 256 被截断）时保留当前列表、不盖活水位，并给出显式重试。
- 本机桌面桥与投递调度：2 秒合并、30 秒发送槽、免打扰、presence 抑制，以及 loopback claim/revalidate/report/click。投递历史 7 天 / 最多 2,000 行；仍 open 的项在批次终态后不再周期鸣响，仅在启动或免打扰结束时补发一次摘要。
- 远程 Web Push v2 已可用（生产 local API 打开 `policyV1` 与 `pushSettingsV2`，共用 `PresenceManager`，远程门打开后在 connecting/online/disconnected 上恢复）。生产 `push_transport` 为 `policy_v2`。出站发送与测试仍受远控激活门约束（`off` / `activation_gated` / `native_unavailable` / `trust_mismatch` 拒绝）；订阅同样受该门约束，解绑仍可用于清理。隔离测试用假 HTTPS 服务，没有真 APNs/FCM。
- macOS 原生适配器在 Tauri 进程内（UNUserNotificationCenter、点击意图、Dock 角标、焦点桥）。`NATIVE_DELIVERY_QUALIFIED` 仍为 false，冷启动点击与签名包门未过。桌面与本机浏览器「发送测试通知」仅在 `native_delivery_v1`、系统许可与设备开启同时成立时可用。入队测试报告 queued，不是系统已展示。托管远程测试走远控门、联系人与订阅。
- 信使把待处理状态标在会话列表上，不再有通知铃铛或通知页。macOS 系统通知和远程 Web Push 仍在：点开进入对应会话，泛化待办推送回到会话列表。最近一条是失败或中断的会话，在列表行上标出该状态。设置里的通知仍决定哪些事件会发系统通知。


- 手机聊天建议保持单行横向滑动，长标题省略显示，点击后填入完整提示。建议背景限制在输入区内，长草稿按视口限制高度并在框内滚动，减少对聊天内容的遮挡。

- 重新设计移动端会话列表顶部：已归档会话入口移入左侧工具菜单中（与日程图、终端并列），彻底移除顶部右侧挤占空间的文字链接；搜索栏自适应撑满剩余宽度，形成通透的胶囊入口。进入已归档视图后顶部提供专属导航页头与返回按钮。

- 手机底栏的会话、工作区、设置切换不再滑动。点一个入口直接换到那一页；搜索、新建、设置分节这些页面自己的进出滑动还在。

- 修正移动端聊天输入框文字偏下的问题，占位文字和单行输入与附件、发送按钮垂直居中对齐。

- 移动端日程详情弹窗居中显示，保留完整圆角，长指令在弹窗内滚动。日程页头标题居中，日期翻页与视图切换分行排列，Bot 筛选可展开收起并显示当前选中数量。

- 手机上工作区和设置按底栏的左右一起切换。工作区在设置左边，所以打开设置时它从右边进来、工作区向左让出；打开工作区时它从左边进来、设置向右让出。回到会话列表时，打开的那一页仍往右滑出去。

- 日程图作为独立页面显示。桌面端点击任意会话会切到聊天，点击打开日程前的同一会话也能切走。移动端页头提供纯箭头返回按钮，刷新及浏览器前进、后退保持页面状态一致。

- 应用里有终端了，Bot 执行命令的过程也看得见了。侧栏底部一个按钮打开你自己的 shell 会话：cwd 默认在工作区但不关在里面，不走批准——批准管的是 Bot 的危险动作，不是你在自己机器上干的事——Bot 看不见也用不了它。守护进程持有它，所以关窗之后 `pnpm dev` 照跑，退出才停；守护进程没了就标成中断，不假装还活着。手机上打开终端会立刻报「执行主机不可达」：终端的字节和事件走的是同一条通道，而手机那侧把它们喂给了只认有序事件的读取器，一帧就把连接判成断链。两条读取路径现在共用同一条分流规则。手机上这两个入口收在搜索行左边的一个按钮里，点开是「日程图 / 终端」——桌面那排页脚图标在手机上并不显示，而原先单放的日历图标又和搜索框的放大镜叠在了一起。配对过的手机打开的是同一批会话，底下一排键补上软键盘发不出的 Ctrl-C、Ctrl-D、Tab、方向键和 Esc。终端在手机上是整页，所以底栏让位——否则它正好压在那排键上——内容四周留出槽位，上下各让开刘海和home指示条。另一件事是 Bot 的命令：输出现在就在它那条气泡下面边跑边滚，命令结束折叠成一行——跑了什么、退出码多少、花了多久。Bot 那个壳一点没变：stdin 仍是关的、十分钟超时仍然杀、每次 spawn 仍然重新判定路径，只是不再是一片静默。输出走自己的通道而不是事件流，而且只发给开口要的客户端，所以面板关着的构建不会去打手机的射频。

- 一次模型选择现在带着这一轮实际做了什么，链结束时也能给 Bot 留一句下次用得上的。模型选择记录在终态旁边写着跳数和工具错误。一条归咎模型的复盘，后来会标出下一次选择有没有照做、纠正有没有变少，或者照做两次仍无改善、已经退出选路。链结束在模型的错上，或者工具报过错但最终做完了，会再发一次短调用：它只能写一条记忆，或改这个 Bot 已有的技能；记录上写明这次记下了什么，或者没有可留的结论，这次调用不在对话里留一行。这样写下的记忆和技能，在 Bot 上写着之后有几次同类任务、其中几次更短。

- 名册上多了一张只读的日程图。侧栏底部（手机上在搜索旁边）有一个日历按钮，打开后把每个 Bot 的每天和每周日程铺在同一张周视图上。格子是规则展开，不是每次运行的记录，钟点是执行 Mac 的本地时间。筛选和格子上都有这个 Bot 的头像和名字，点一块能看标题、时间和任务指令。这一版还不能拖；手机上写明改时间要到桌面，或打开 Bot 自己的日程。底栏仍是会话、工作区、设置。

- Bot 设置的分节贴着抽屉边缘。此前那一排是圆角胶囊，上下左右都留着一圈空隙；五个分节现在铺满标题下面的整行，当前这一节用强调色下划线标出来。

- 一件事可以看成一张流程图。会话顶栏在「模型选择记录」旁边多了「经过」，消息菜单里有「看这件事」，一条消息底下那个 Bot↔Bot 私聊的来源头上也有。桌面端这块板是浮动窗口，拖标题栏就能挪开，拖任意一角能改大小，下次打开还停在上次的位置和大小；换一条对话，窗口里的流程图换成那条对话的。对话在它后面照常点。点一个节点，对话切到那一轮所在的会话并高亮，窗口留在原地，所以顺着节点点下去能看到整条连锁；当前会话里的节点加一圈标记。手机上是单独的一页，铺满屏幕，不能拖也不能改大小。三处打开的是同一张板，从上往下读：你说的那句，然后它叫醒的那些轮，再然后那些轮叫醒的下一轮；同一轮叫醒的人并排。一张卡片上挂着这一轮交出的文件。点卡片跳到转录里的这一轮。点文件，它接在交出它的那一轮后面展开，是流程图的下一站：只读，看内容，没有聊天旁边那条预览的文件树、源码和保存。再点一次同一个文件、它自己的 ✕，或先按一次 Escape，就收起来，板子还在。群、私聊和围绕同一件事的 Bot↔Bot 私聊是同一张图，因为它们本来就共用一个工作目录。这张板是把已经发生的事读出来，不是一份计划，上面的东西不能拖、不能勾、也不能停。手机上同样是从上往下。

- 经过图现在是它本来的形状，那块板也成了画布。群里你发一句，被点到的人是一起醒的，每一轮又可能叫醒别人，所以图是分叉的，不再是一路往下的一条；连线画的是真正的叫醒关系，而被这张板之外的事叫醒的那一轮，会写出是谁。以前的事在启动时补一次归属，老的轮次不再堆成一坨；一轮交出的文件挂在交出它的那一轮上，你自己那句也一样。分层交给 dagre，同一轮叫醒的几个人，从左到右按发生的先后排。板子不再有滚动条：它是一个固定视口，拖着平移，没有边界；滚轮以指针为中心缩放，手机上两指捏合。缩放、平移之后排版不会再挤成一团。节点里的产物可以全屏——桌面上盖住整个窗口，手机上铺满一屏——Escape 一次退一层：全屏、文件、板子。手机上全屏是一页：返回先退出全屏，流程图还在，再按一次才是离开流程图。浮窗四角都能改大小，拉哪个角，对角就钉在原地；角上什么都不画，鼠标移过去变个光标，就是全部的提示。

- 手机上那些其实是表单的弹窗，现在都是页面。新建 Bot、新建群、编辑记忆、编辑技能此前都是浮在屏幕中间的卡片：四边各留几像素背景，键盘一升起来占掉剩下的一半，而卡片的圆角和阴影是围着一块你本来也看不过去的地方做的装饰。现在它们整屏进入，页头 52px（让开刘海），右上角的 ✕ 换成和别的手机页面一样的返回箭头，底部是一个整宽的主操作按钮——原来右对齐的「关闭」去掉了，返回箭头就是它。进出用和其他页面同一条滑动。模型选择记录也一样：此前是 318px 宽的侧边抽屉，旁边留着 72px 读不到也点不着的对话，现在铺满整屏。首次向导和远程配对的卡片在手机上也不再内缩，直接是一页。

- 确认框仍然是居中的对话框，但按下去更容易了。回答「要不要清空历史」这种问题不该把人带离原来那一页，所以危险操作、独立运行时开关和预览未保存这三类确认没有改成整页；手机上它们改成两个各占一半宽、44px 高的按钮，✕ 也是 44px。

- 手机上点按不再闪出系统那层灰色方块。iOS 和 Android 会在按下的元素上画一个矩形高亮，圆按钮下面是个方影子、列表行上是一整条灰块，而且盖在应用自己的按下效果上面。现在按钮、链接和菜单项都关掉了这层原生高亮，按下的反馈由控件自己给（浮动 `+` 会缩一下并加深颜色）。

- 手机上的新建改成了列表右下角的浮动 `+`。此前每个分组标题右边各有一个 22px 的 `+`（一个建群、一个建 Bot），在一屏最上面、一只手最难够到的地方，两个几乎一样的小加号还得先认清哪个是哪个。现在一个拇指够得到的圆按钮，点开就问要建什么：「新建 Bot」还是「新建群」，选了直接进原来的新建表单；再点一下、点旁边、按返回键或 Escape 都收起它（菜单总是第一个被返回键收起，不会连着退出列表）。按钮只出现在会话列表这一页上——打开某个会话、归档视图、搜索整页、工作区和设置里都没有它，抽屉和弹窗也压在它上面。宽窗口照旧：分组标题里那两个 `+` 留着。

- 手机上会话列表这一列的左右边界对齐了。搜索框此前比下面的头像和分组标题靠左 10px，归档入口的箭头又比行里的时间靠右一截，列表还被滚动条再推进去 15px——三条边各走一路。现在搜索框、分组标题、头像都从同一条线起笔，归档箭头、标题右侧的「还有 N 个」、行时间和 Bot↔Bot 那条「来自 …」都齐在右边同一条线上；那条来源行原来悬在标题左边 14px，现在和标题对齐；手机宽度下列表不再为滚动条留位置（触摸滚动本来就有自己的指示）。

- 手机上，Bot 设置里的技能数量和群组设置里的成员数量贴在行尾箭头旁边。此前这两个数字停在行中间。

- 手机上的搜索是一整页。此前点一下列表顶上的框，命中就挂在框下面那一小片下拉里：键盘一升起来遮掉大半，一条命中的两行摘要挤在 300 多像素宽里断成四行，底下那半截会话列表还在，看着像你根本没离开列表。现在那个框只是入口，点开是一页——字段在页头，旁边就是返回，命中从页头一直铺到屏幕底部，底栏在搜索时让位，输入框自动取焦。这一页不进地址栏：返回键收起它、清空输入，再按才真的离开列表（返回一次只退一层，和其他手机页面一个规矩），进出用和别的页面同一条滑动。离开就算结束这次搜索，所以回到列表时框是空的，不会拿上一次的问题接着搜。命中本身也不再摆 markdown 源码了：`### 标题`、反引号、`**加粗**` 和链接语法都剥掉，只留说过的话——宽窗口的下拉一并如此。宽窗口照旧是框加下拉，两种形态共用同一份命中列表和键盘操作（↑↓ 选、回车打开）。

- 手机会话顶栏改为无边框、整高可点击的返回与更多操作区域，头像、文字及中间留白均可打开详情。Bot 名称下方只保留紧凑的状态和模型，不再重复显示双方身份；长名称和长模型名自动省略，不挤压导航按钮。桌面顶栏保持原样。

- 与 Bot 单聊时，消息旁不再重复显示 Bot 和用户自己的头像，包括流式回复、提问和中断提示；群聊和 Bot 间会话保持原有头像展示。

- 手机上的会话列表按常见 IM 的样子重排了。每行现在是「头像 48px + 名字与时间 + 最近一句与未读数」两行结构，行高 68px，卡片式的圆角边框换成从文字左缘起笔的细分隔线；时间按远近变化——今天给时刻、昨天给「昨天」、一周内给星期、再往前给日期，未读时时间也转成强调色。最近一句现在是纯文本:`#` 标题、反引号、列表符号、加粗斜体、链接语法都会被剥掉，代码块缩成 `[ts]` 这样的标记，不再把 markdown 源码摆在列表里。「空闲」这类常态状态不再逐行重复(头像上本来就有状态点),只有 Bot 真在忙时才在第二行顶掉预览显示「正在回复」，和 IM 里的「正在输入」一个位置。置顶区为空时不再占一行。列表上方的那几层也瘦了下来：搜索框和归档入口此前各占一行、加上空置顶区，第一个会话要等到屏幕两百像素以下才开始——现在搜索和归档并排在同一行 40px 里（底栏已经写着「会话」，这里就不再重复标题；只有归档视图自己留标题），第一行回到 87px，一屏能看到八个会话而不是五个。行的几何只在手机宽度下变化，宽窗口的列表照旧——纯文本预览是列表共用的，宽窗口也一并不再显示 markdown 源码。

- 手机上 Bot 消息的排版重新组织过。名字上方那一行此前会连着换两次行——Bot 名字折成两行、模型名在 `gemini-3.8-/flash-high` 中间断开——而悬浮的复制/引用小条又正好压在上面。现在这一行不折了：名字按需要占位、占不下就省略号，模型名交给会话顶栏（那里本来就写着），时间和耗时并排贴右；复制和引用改由长按菜单提供，不再有东西盖住气泡。正文里列表的圆点离文字更近，换行后的第二行跟正文对齐而不是像新起一条；提交号、长路径这类长词现在会在需要时断开，不再自己占掉一整行。你自己发的消息不再在旁边重复画一个头像，每行多出 42px 的宽度。

- 设置里的 ✕ 现在关掉的是它所在的那一页，不是整片设置。以前在手机上点进某一节、或在模型端点/MCP 的编辑页里按 ✕，整个设置页就没了，得从头再点回去；现在它退一层——编辑页回到列表、分节回到分组列表，只有最外层那个 ✕ 才关掉设置。Bot 与群组设置同理：抽屉里的 ✕ 先关掉打开的分节或成员资料，再关抽屉。

- 手机上的返回重做了一遍，现在一次只退一层，而且退得动。此前**关闭**一个界面也会往历史里压一条新记录：用 ✕ 关掉 Bot 设置后按返回，它又原样打开，再按又关上，来回弹。现在打开才压记录，关闭要么退回当初打开它的那一条、要么改写当前这一条，历史不再长胖。不在地址里的层（新建 Bot/群的表单、技能编辑、模型端点与 MCP 的编辑页、设置和 Bot/群的分节、模型选择记录、线程抽屉、确认框、主题菜单）由应用自己按由内到外的顺序收起，返回键先收它们，收完才真的导航；进行中的确认框会把返回吃掉，不让它半路取消。地址里带的层（会话、设置、工作区、预览）交给历史本身，所以返回和地址栏永远一致；只有工作区或预览里有未保存的改动时才会拦下返回，先问你保存还是丢弃。直接用链接打开某一层时，返回不会把你甩出应用，而是改写那一条记录。

- 手机上的页面切换有了一致的方向：新页从右边滑进来，**返回就沿原路往右滑出去**，退回的动作正好是进入的反向，0.22s。此前只有工作区带一个从右滑入的动画，设置页是瞬间出现，会话没有——三个入口的手感各不一样；中间试过按底栏顺序左右滑，但只有从底栏点进去时方向才对，用返回键、✕ 或会话被删掉时方向就反了，所以改成对称的一条路：底下那一页不动，只有最上面那一页走。工作区在手机上也不再压一层暗背景。较宽窗口照旧（工作区仍是右侧抽屉的滑入，设置仍是淡入），开启减少动态时都不动。

- Bot 输出的外部链接支持点击在系统默认浏览器或新标签页中打开，并渲染对应的外链标记图标，与内部工作区文件链接明确区分。

- 消息右键菜单会阻止右键和 Control 点击触发的自动选词，保留手动选字和复制功能。

- Bot 与群组的设置在手机上也成了整页、分级进入的样子，和全局设置页一致：先是一屏分组列表（Bot 是基础信息 / 技能 / 日程 / 记忆 / 操作，群组是群资料 / 成员 / 会话操作 / 危险区域），点进去才是那一节的内容，标题旁边就是返回。抽屉此前在手机上是 94vw 的侧边抽屉，边上留着一条背景；现在铺满整屏，进入分节时让出自己的标题栏，只留一个标题。系统返回键先退回列表，再关掉设置。较宽的窗口一切照旧：Bot 还是顶部标签页，群组还是一列卡片。

- 历史很长的会话，打开和使用都不再卡。转录此前把载入的每一条都挂在页面上，而且快照一变就整屏重建：两千条的会话里，流式输出的每个 token 要花约 2.5 秒，打开则要近 2 秒。现在只挂载最新的六十条，向上滚动再逐段展开——不管历史多长，打开约 80 毫秒、每个 token 约 1.5 毫秒。气泡复用已经渲染好的 markdown，不再逐条重新解析；引用和耗时查找按消息列表建一次索引，不再每行扫全量。第一页之外的历史也终于能看到了：滚到顶部先补已经载入的消息，再由「载入更早的消息」向 Mac 要上一页。首页还在路上的会话会明确说「正在载入」，而不是先给一个空房间；正文特别长的一页会按字节截短以装进中继的信封，不再整页发不过来。

- 工作区和预览面板的文件树在远程加载期间显示加载提示，区分空目录与加载失败，并提供重试入口。重复展开正在加载的目录会复用请求，过期响应不会覆盖新的工作区或任务文件列表。

- 手机浏览器返回会从设置编辑页逐级回到列表和设置首页，内层页面退出后再离开设置；确认操作期间保留当前页面。

- 打开工作区文件时不再对着空白预览干等。面板会显示加载环和进度条：已知总大小时按比例填充（含远程 Noise 分块），未知时扫条并显示已收到的字节。

- 手机底栏调整为带文字、选中状态及安全区留白的「会话 / 工作区 / 设置」三个主入口；归档入口移至会话搜索下方，外观从基础偏好进入。详情页收起底栏；切换离开文件编辑时保留保存、丢弃和取消流程。

### 桌面

- 开发态桌面编译不再对 gated 的独立运行时 launchd 辅助函数报 unused。生产仍 fail-closed；开发态仍不安装作业。

- 设置可选择独立 LaunchAgent 运行时，**默认关**。生产仍 gated（`g_pack_not_verified`）；开发态不安装作业。开启顺序为排空 → 停窗口 Spawn → 无闩退出 → 等待端口空 → bootstrap 新子进程；失败保持 Down。浏览器或原生门拒绝开启时，确认后关闭对话框并保持开关关闭，不再留下空确认框。残留 `runtime.independent` 不会在策略不可用或作业未加载时关掉窗口监督。独立模式已开启时退出窗口不会停 agent；显式 stop 写 `runtime.stop`，PathState 不再拉起。交接类 IPC 仅 bundled 主文档可调。`launch_at_login` 仍是窗口登录项，与运行时作业独立。G-pack / G-launchd 未通过。

### 日程

- 日程卡片区分未知/待确认请求与版本冲突，提供复用原内容和编号的“重试原请求”。重试后保留未发送的修改草稿，核对列表并重新打开后再保存。

- 日程写入共用既有回执与同步边界：要求版本的 PATCH/DELETE 在事务内只比较一次，scheduler claim 单调推进版本；客户端未知结果保留请求 id 供显式重试，不从 HTTP 返回行写入状态。

- 删除/清空提交状态绑定确切的确认框。旧操作提交事件先移除对话框、HTTP 后返回时，后来打开的确认仍可操作，不会被旧回复清除或解除提交中状态。

- 较早日程搜索不再覆盖后来打开的编辑草稿或重新打开已关闭资料；所属 Bot 已删除的历史日程在搜索中明确标为不可用，不再点击后无声关闭搜索。
- 共用删除/清空确认框隔离背景操作、限制键盘焦点并在关闭后归还触发控件；保存中不能取消或重复确认。确认按钮与日程星期标签均至少 44×44px。

- Bot 资料新增日程增改查删：每天/每周指定星期、Mac 本地时区说明、暂停/恢复、删除确认、校验及提交中/错误态。搜索打开所属 Bot 与日程编辑入口，事件和快照保持多客户端及重连一致；有改动的草稿保留原版本，并发修改后需明确载入最新版本。日程 PATCH/DELETE 接受可选 `if_revision`（`updated_at`），旧版本写入返回 409。

### 本机同步

- 凭据重试按各自操作与事件顺序跟踪：较旧的空待办列表不能丢弃较新的待写新建请求，迟到失败不能复活已完成请求；修改载荷的提示不抹掉已确认状态，修复/取消或终态回执同步清理载荷与横幅。

- 事务回执与同步事件共用唯一提交边界：提交后先发布当前状态，再运行文件/引擎副作用，不跨凭据等待持有 SQLite 事务。凭据待办、修复和取消状态在多个客户端同步；旧重试返回不能影响替代连接。

- 会话初次历史尚未载入时搜索该会话，会先走替代详情加载再分页；连续搜索以最新目标为准，不再取消唯一的历史载入而只剩最后一条摘要。

- 初次连接等待快照期间保留 URL 指定的设置、会话/Bot 面板与工作区浮层；旧分页搜索在选择、历史或连接变化后返回，不再覆盖当前高亮或把先前会话标为已读。

- 会话详情即使先于 WebSocket 到达，也会等事件流到达其水印再安装；重连不再等待旧会话请求，迟到的变更结果或失败不能回退状态或断开替代连接。
- 仅修改端点行的提交也发布派生设置，确保各客户端的向导完成状态、默认模型同步，包括后续凭据操作失败的情况。

- 信使先订阅，再装载单一带水印的快照，包含记忆、日程与 Always allow 规则；切换会话也走同一屏障，避免较晚返回的 HTTP 数据覆盖实时更新。
- 每进程随机事件游标与最多 2,000 条 / 16 MiB 的回放环检测缺口、溢出和守护进程重启，自动重取快照。持久事件紧随 SQLite 提交，覆盖 Bot 工具及等待钥匙串的写入；部分回复从轮次绝对文本重建，不回放 token。
- 旧本机 WebSocket 客户端仍接收原始事件帧，新信使显式协商 `sync-v1`。本次仅提供本机同步基础，不开启远控运输。

### 实验性远控协议

- 手机断线丢掉的一次保存，不再让之后每一次修改都失败。结果未知的请求会占住它的槽位，好让改过的载荷不能悄悄顶替它——但这个槽位此前永远不会释放：一次掉线的保存之后，再改 Bot 的模型就一直冲突，刷新页面也还在，界面上也没有出口。现在客户端会先问 Mac 那个请求到底怎样了：没有回执说明业务事务根本没提交，槽位直接让给新载荷；回执存在时，改同一个 Bot 可以接着改（同一实体再 PATCH 一次不会产生重复），而新建仍然要显式重试或退休，避免变成两条。问不到 Mac、或凭据写入仍在排队时，照旧阻塞。
- 设置页已经开着时，「已连接设备」会自己把列表取回来。这个列表是单独取的，不在快照里，而以前只有点开设置的那个按钮会去取——于是从 URL 恢复出设置页的刷新（开发时的热更新、手机上的刷新）会让卡片一直显示「还没有已连接设备」，直到关掉设置再打开。现在只要主机报告在线，不管设置页是怎么开的，卡片自己会去取。
- 托管应用会发现 Mac 发布了新版本。手机上一个页面可能一开就是几天，而此前没有任何东西告诉它代码已经变了：现在每三十秒查一次 `_app/version.json`，页面回到前台时也立刻查一次，发现新版就在底部浮出一枚「有新版本 / 刷新」的小胶囊，而不是把你正在看的页面直接换掉。这枚胶囊让开手机底栏，不再被压在它下面，而且可以关掉：关掉只表示「这一版先不」，服务器再发新的一版还会再问。桌面窗口不受影响，它走应用内更新。
- 手机端设置改为全屏布局，首层按常见移动设置页展示分组入口；进入通用、基础偏好、模型服务、MCP 扩展或关于后，打开带返回操作的独立详情页。模型端点与 MCP 服务器的新增/编辑会继续进入第三级全屏编辑页，分别提供返回列表和关闭设置动作。较宽窗口继续使用左右分栏及弹窗编辑器。
- 装到主屏幕的应用重新有图标了。三张 PWA 图标是在 64px 上画好后原样贴进整张画布的，于是文件里 98% 是白色，主屏幕上看着就是一块空白；启动画面同理，因为它正是这张图标配近白底。现在按桌面应用同一套品牌标记重绘——青色渐变上的两个队友——由系统自己蒙圆角的那两张改为全出血；启动底色换成品牌色，不再用只适合浅色主题的近白。manifest 的 id 原本写死成 real-bot.app 的地址，自托管的源站只能忽略它，现在改成相对值。
- 手机上预览作为盖住会话的一层打开，它的文件列表是独立的一屏，不再是文件旁边的一栏。「铺成一层」这件事现在归外框管，所以本来就是全屏的工作区浮层不会再在自己里面套第二层。文件列表改成预览上方的下拉，不再是文件旁边的一栏：展开时盖在预览上，预览本身不动；选中文件、点下面的预览或按 Escape 都会收起。
- Mac 设置里的「已连接设备」会用相对时间显示每台设备最近一次建立加密连接或发送请求的时间；设置打开期间每 30 秒刷新，未来时钟偏差按当前时间显示。可通过本机确认移除指定设备；移除会推进信任代际、关闭该设备连接并撤销后续访问。
- 手机上的产物预览改成盖住屏幕的一层，不再是输入框下面追加的一段。它原本占栅格里的一列，窄屏时变成会话下方的一条，再把 390px 劈成左树右文件——点开的那个文件最后挤在比它文件名还窄的一栏里。现在它铺满屏幕，标题下是路径，头部一个列表按钮在「这个文件」和「同批文件」之间切换，不再有拖拽分隔条；在列表里点一个文件就直接回到它。
- 手机会话顶栏改为单一操作菜单，标题获得完整空间；下一步建议在输入框上方换行排列；空输入框默认更矮，50 MiB 远程附件提示放进框内。
- 连接画面上的加载环重新转起来了。样式搬进组件时它的 keyframes 落在了外面——Svelte 的 keyframes 是组件作用域的，于是规则还在、它引用的动画没了，环从那以后一直是个静止的圆圈。
- 「连接中」和「不可达」不再是同一个画面。还在尝试时加载环继续转，并写明「正在连接运行时…」（远程设备上是执行主机），只有连续几次失败才变成「连不上运行时」，而且这个画面现在带「重试」按钮，点了立刻重试，不用等退避走完。连接断开时先按重连显示，不会 Wi-Fi 一闪就断言 Mac 没了。开启减少动态时圆点不动。
- 手机端布局整过一遍。消息区给输入区留的位置改为按它的实测高度走——原来是按桌面输入条调的常数，遇到更高的（建议条、附件芯片、换行占位符、底部安全区）就会盖住最后几条消息。输入区也会浮在软键盘之上：iOS 上键盘弹出时布局视口不缩，原来这一条会被压在键盘后面。会话顶栏的三个按钮在手机上只留图标，名字不再被压成两个字加省略号；会话列表底部四个图标改成 44×44 并均分排布，不再是缩在两角的 30px；Bot 资料页的分页标签也够高了；建议条在可滚动的那一边做了渐隐，不再像被切掉半个芯片。
- 窄窗口下界面恢复可用。低于 680px 本该塌成一栏——先是会话列表，选中之后是对话——但做这件事的规则写在桌面栅格**前面**，而媒体查询不增加特异性，于是三栏栅格胜出：200px 的列表旁边挂着一条死带。现在窄屏规则排在它要覆盖的栅格之后，线程抽屉改为盖满屏幕而不是堆成第二行，输入区的手机样式也不再被同样的方式盖掉。
- 主机掉线后，设备端重连改为退避（1 秒翻倍到 20 秒，带抖动），不再每秒重试。中继对每个地址只给每分钟十次 WebSocket 握手，原来的节奏十秒就把配额烧完，剩下的时间全在被拒——越急着重连恢复得越慢。连上之后延迟复位。
- 开发态点配对不再直接失败。桥会先试打包窗口那条设置通道，而那条命令拒绝未签名的窗，于是配对报「开配对窗口失败」而不是落到开发路由上。卡片本身也重做了：状态改成卡头上的徽章而不是混在段落里，没有设备时空态本身给出操作，配对过程按它本来的三步呈现——粘贴配对内容、核对主机指纹、批准到来的设备——决定和退出放在做决定的那一步末尾。失败时给的是「重新开一个窗口」，不再只有关闭。
- 配对设备现在可以在设置面板里完成。远程卡片开配对窗口、给出带复制按钮的配对内容和设备上应当显示的主机指纹，设备提交后显示它报的名字和指纹并等你批准。配对内容本身从 399 字节 JSON 缩到约 205 字符：字段改成打包的二进制，中继地址干脆不进编码——读它的设备本来就由那个 origin 提供服务，于是粘来的内容没法把设备指到别的中继去。旧的 JSON 仍然能配。打包态这张卡走窗口的设置通道与 Touch ID，源码态走开发通道，而开发通道只开放配对：改中继、重置身份仍然只能在 socket 上做。
- 清掉浏览器站点数据后，托管信使不再卡在「执行主机不可达」。活动的 IndexedDB 连接被强制关闭后读登记会抛异常，之前这会让重试循环直接死掉：页面一直挂着不可达横幅，不回到配对页，只有把应用彻底关掉重开才恢复。现在读不出来的登记按没有登记算，某一轮抛异常也仍然会排下一轮。
- 新增仅开发可用的配对路径，可在 G-pack 之前把协议真正跑通。源码态 daemon 带 `REAL_BOT_DEV_REMOTE=1` 启动时，用数据目录下 0600 的凭据文件替换密封原生凭据，并把窗口那条设置通道开在 0600 的 unix socket 上；`apps/daemon/scripts/dev-remote.ts` 在终端完成查看状态、初始化中继与配对，批准设备前先打印 Touch ID 弹窗本会显示的内容。编译后的 daemon 永远不走这条路径，生产激活门原样未动：密钥不进钥匙串，终端确认不等于用户在场，G-pack、L1、G-uv、G-push、S-rev 一个都不因此通过。
- 可选 Web Push 仅由 Mac 发送泛化待办提醒（载荷 `{t:"pending"}`，固定文案「Real Bot 有待处理事项」）。订阅按设备保存 HTTPS endpoint URL，不能只存哈希。出站仅允许 `*.push.apple.com`、`fcm.googleapis.com`、`updates.push.services.mozilla.com`，`redirect: error`；中继不代发。点击只打开 PWA 并拉收件箱，绝不批准。拒绝、过期或吊销不丢待办。之后在已有 bot / ask / approval 行上点反应不会再发一条推送。iOS 16.4+ 主屏幕 standalone 才是 G-push，**尚未通过**；普通浏览器冒烟不算该门。
- 远程维护仅走端到端加密：状态（版本/模式/可达性）、脱敏诊断（只有计数与错误码，不含 Bot 名、正文、路径或密钥）、排空后重启、强制重启、停止与吊销其它设备。重启等捕获的活轮集合结束后由本调用方留回执并无闩 exit 0，由当时窗监督者拉起。强制需要新鲜 WebAuthn UV，超时或掉线绝不自动升级。停止先写闩再退出且保持停止。窗不在且未独立时重启不可用，不安装后台任务。窗监督重启仅在本机继承的桌面通道真正挂上后才可用。UV 已提交后输掉 busy 的重启/停止会把该请求回执落成 409 并删掉生命周期行，不再留下 202 pending。本机回环仍无维护写路由。G-uv / 真机门**未通过**。
- 已提交的远程附件 POST 用同一 `request_id` 重放时返回首次 201，不再把无暂存文件的 JSON 重试当成不同载荷。超限文本 PUT 在 Noise 上保持 `too_large`。真实 TCC 与手机下载未通过。
- 远程目录浏览与限流文件双工：`list_host_dir` 复用 `classifyPath`/realpath，拒绝其它用户 home，TCC 拒绝返回类型化「在 Mac 上授权一次」错误。改工作区根仍须一类本机确认；逐级浏览不需要。上传/GET 走 type 0x05/0x06、50 MiB、单设备并发 2、tmp+fsync/TX/rename，ETag 为明文 SHA-256 且 PUT 必带 If-Match。远程 Finder 改为下载/复制相对路径。真实 TCC 与手机下载未通过。
- 新增托管信使远控客户端：与 LocalApi 方法面一致的 Noise RPC、钉住主机 DH/签名公钥与中继 origin 的扫码配对、仅 IndexedDB 存设备身份，以及 standalone PWA 壳。托管生产包（`REAL_BOT_HOSTED=1` / `pnpm --filter @real-bot/messenger build:hosted`）不含 `__local-api` 与本机 bearer。远程 URL 只允许会话/浮层/附件 id（`?s` `?o` `?b` `?a`），禁止 `?p=` / `?w=` 文件路径。Service worker 只缓存带哈希的 immutable 资源，导航 index 仍 no-store。断线显示执行主机不可达，不排队命令；未发送内存草稿重连需确认，未知结果查同一 `request_id` 回执。WebAuthn `create`/`get` 使用有效域名 `rpId` 且要求 UV，仅在 Split 后登记；失败仍可普通聊天，无点击确认 fallback。公网配对仍默认关闭。S-rev、G-uv 与真机 L1 **未通过**。

- 远控加密 RPC 在副作用与回执前拒绝 `constructor` / `__proto__` 等原型字段名，并把 schema 失败映射为 422，不再因此关闭 Noise 会话。本机确认使用独立的 `renew_first_uv` / `recover_trust` 种类，不再复用配对或更换中继。

- 修复 daemon 复核问题：等 epoch 恢复同样提升防回滚高水位；原生确认的身份重置、中继/工作区切换、首次 UV 续期绑定当前公钥与耐久转换状态。读取类模型请求在凭据等待后重检取消，已接受轮次仍独立运行。主机级限速与吊销确认进度避免超出中继 burst，文件取消跟踪实际 EOF；远控属性、Stop 幂等和稳定错误码严格验证而不改变本机兼容性。生命周期回执在副作用成功前保持 pending，强制排空计入未结束工作并阻止后续工具。生产/原生/审计门仍关闭。

- 新增原生门控的 daemon 出站中继/Noise 接线、共享业务分发与同步回执、SQLite 信任/重放/UV 状态、分方向签名配对回复及仅打包本机窗口可用的设置通道。每次设备吊销均提升钥匙串防回滚高水位，旧会话与进行中的读取主动关闭/重检；共享排空暂停新轮、保留已有批准/回答，永不自行退出。stock Bun 仍不合格，原生激活、公网配对、PWA、真机与独立审计门**未通过**，不宣称远控产品可用。

- 本机回执改用 `@real-bot/remote/canonical` 的完整六字段摘要、附件排序与 SHA-256，不再保留第二套生产编码器。既有回执字节、严格路由/If-Match 校验及显式重试行为不变；有空洞的附件数组和非字符串/异步编码结果在业务副作用前拒绝，编码Promise挂拒绝处理但不等待。远控能力仍禁用。
- 中继 HTTP→HTTPS 跳转在拒绝实际查询串后保留原始转义路径，避免编码的 `?`、`#` 和百分号变成 URL 分隔符或改变导航含义。目标主机仍固定；真实 Caddy TLS 回归覆盖编码路径与查询串不反射。
- 修复主机链路原子绑定时遗漏等待设备过期、HTTP 跳转前查询串反射，以及 immutable 资源缓存头被覆盖的问题（缺失资源与错误仍不缓存）。浏览器 fixture 复用生产 CSP 生成器，中继邮箱限额复用共享协议契约。新增可复现的本机 Caddy HTTP/HTTPS/WSS 测试，验证真实响应头、可信 localhost 测试证书、真实 Noise 往返与吊销，不冒充 Docker/公网域名验收。
- 新增默认关闭的自托管 Bun 中继：耐久一次性 bootstrap、存储公钥 Ed25519 挑战、有界未登记设备配对邮箱，以及认证主机控制与每设备出站链路，仅转发 Noise 密文。真实 socket 测试覆盖吊销/离线拆链、帧/IP/带宽/背压限额和仅元数据日志。非 root Compose/Caddy 模板提供生产静态信使壳，不代表远控/PWA 客户端完成；Docker 引擎、域名/TLS、独立复核与真机门仍待验证。见[部署与适配器契约](docs/deploy-remote.md)。
- 修复 Buffer 密钥别名：握手失败、Split 和关闭不再擦除调用方身份或破坏并发会话；二进制解码正确处理 Buffer 视图偏移。WebAuthn 同时拒绝非法 JSON 语法及重复字段。共享回执摘要加入规范条件头（含 If-Match），允许同名附件并按文件名字节、内容摘要排序；此前未发布摘要不兼容。内部辅助函数不再公开，HKDF/字节编码复用已钉定 Noble。远控、真机与审计门不变，仍默认关闭。
- 新增浏览器/Bun 共用的远控密码原型：钉扎身份与签名 Hello 的 Noise IK、有界加密帧、配对授权、中继挑战证明、规范回执摘要，以及绑定完整操作、一次性 challenge 的真实 COSE/WebAuthn 验证。通过官方协议向量及独立 Rust snow 对端检验。远控运输与公网配对仍是提案、默认关闭：真机 PWA/WebAuthn 与独立安全复核尚未通过。接口见[协议契约](docs/remote-protocol.md)。

### 原生远控基础

- 桌面及根类型检查覆盖原生打包脚本，PR CI 的 macOS 任务运行 stock runtime 不合格回归；不启用远控凭据。
- 应用包元数据要求 macOS 13.0+，打包时检查必需原生产物的最低系统版本。本机确认 IPC 拒绝开发页、远程页和产物文档；认证和存储后只报告证明剩余有效期。macOS CI 与发布验证增加无凭据 Swift fixture；stock Bun 仍未合格，远控能力保持禁用。
- 新增 Swift Security/LocalAuthentication helper、双向鉴权的版本化本机 socket、绑定动作的一次性 Mac 确认，以及 daemon 的窄原生凭据读取/高水位更新接口。远控凭据使用不同步、仅本设备的数据保护钥匙串共享组，不混用不兼容的旧式 ACL 属性。
- 打包 helper、原生库与编译 daemon；发布版不再依赖 PATH Bun，除非显式选择源码模式。远控/独立模式仍禁用：稳定签名、封闭运行时（拒绝 stock Bun 的解释器逃逸）、合法共享 entitlement 与干净 Mac 的 G-pack 验收仍未完成。源码开发没有自动通过的认证回退。

### Daemon

- 修复嵌套工作区文件、深层目录展开和带斜杠搜索被路由校验误拒绝的问题。无工作区时经内部 inbox 别名上传的附件重启后仍可读，不开放数据库目录其它文件。待删除的端点/MCP 凭据不能被修复成孤立密钥，控件只允许完成清除。
- 修复内部 inbox 软链接下并发附件覆盖、坏 inbox 阻塞无关操作、变体路由绕过版本检查、端点改动未失效设置版本的问题。凭据事件提交时立即发布待写/删除状态，完成时发布新状态；旧 MCP 探测不能覆盖新配置。包括 Bot 批准后失败的凭据操作都可显式修复或取消，不重放工具。
- 本机可变接口支持可选的 ULID `X-Request-Id` 和事务回执：同载荷重试返回首次结果，改载荷返回 409，回执正文过期后保留耐久墓碑并返回 410。端点/MCP 凭据使用可续办的待写入状态，回执不存原始密钥；模型探测明确不属于可重试变更。批准接受不再等待外部操作完成，MCP 配置提交后异步更新探测目录。
- 附件和工作区保存采用同目录暂存、fsync、SQLite 提交记录、rename 与启动恢复。文件 GET 返回 SHA-256 ETag，条件保存遇旧版本返回 409；本机无条件保存和 1 MB 文本上限保留。设置提供版本号，注入的业务请求可强制实体/设置版本检查，不新增远控或运行时 HTTP 路由。

## 0.1.0-rc.5 — 2026-09-21

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### 桌面

- **更新现在自己下载、自己装。** 以前发现新版只是把 `.dmg` 丢给浏览器，剩下的全归你：打开、拖过去覆盖、再过一次 Gatekeeper。现在「关于」卡片里直接下载，带进度条；下完校验镜像里那份确实是本应用、而且正好是它许诺的版本，然后替换当前这份并自动重新打开。下载失败或者校验不过，原来那份一点没动；浏览器下载仍在——当这份应用根本没法自己替换自己（别人装的那份、或者从源码跑的）时给的就是它，并且会写明是哪种情况。下载过程中可以取消。
- 检查更新和下载都会走环境里的 `HTTPS_PROXY` / `ALL_PROXY`（`NO_PROXY` 命中的主机不走）。在 GitHub 只有过代理才通的机器上，以前检查更新只会失败成「没能检查更新」，还看不出是为什么。
- 窗口退出后再开，还是关掉前的大小。拖边框、缩放到填满屏幕、藏到托盘或退出，都会把宽高（以及是否填满）写进窗口进程自己的 `window-size.json`；下次启动按这份恢复。不记位置，免得换了显示器窗落到屏外。

### 守护进程

- **一轮的中间产物现在落在一个目录里，不再散在工作区根。** `shell` 不传 `cwd` 就在本轮工作目录（`work/<日期>-<摘要>-<id>`）里跑——下载、转换、脚本产物因此不需要 Bot 配合就不会乱放；目录在第一次真被用到时才创建，只说话的轮次不留空壳。同一件事跨交接、点名、判断下场、Bot↔Bot 私聊和中断续跑都用同一个目录；日程每次触发新开一个，静默六小时后的下一轮也新开。文件工具不变——`read_file` / `write_file` / `delete_file` / `list_dir` 的路径仍相对工作区根，所以壳和文件工具是两个基准，这是有意的；每轮的局面块都会写出工作目录。 Bot 按它壳的视角写出的路径（刚重定向进工作目录之后那句 `sales.csv`）会被改写成能解析的那条，于是转录里的链接可用，也不会挂出一个工作区根上并不存在的幽灵文件。
- **命令产出的文件终于会被列出来了。** `shell` 现在在每次调用前后各扫一遍工作目录，把新增或改写的文件报出来，于是下载、转换、渲染的结果会像 `write_file` 一直做的那样挂到消息上。留下超过 200 个条目的命令则一个都不报并说明——`npm install` 不是一份产物清单。
- **产物入口点开的是整件事，不是一条消息。** 树里列的是这件事的消息引用过的全部路径，以工作目录为根，目录之外引用的路径与它并列平铺，本条消息自己的那几个打点。交接之前上一个 Bot 做出来的文件，现在点一下就到，不用往回翻转录。
- 超出上下文限额的工具结果改为落在本轮工作目录下的 `tool-results/`，不再堆在工作区根那个扁平目录里；守护进程启动时会把关闭超过一周的工作目录里的这份 spill 扫掉，工作目录里的其它东西一律不动。`full_result_path` 仍相对工作区根，所以用 shell 读它要传 `cwd: "."`——`recovery_hint` 和轮次指令都写明了。
- Bot 写进工作目录 `tool-results/` 或 `scratch/` 的文件不再自动挂成消息产物。轮次指令让 Bot 把纯过程文件放进 `scratch/`，这个承诺现在真的成立了。
- 守护进程把启动、拒绝和致命错误写进数据库旁边的 `daemon.log`。此前运行时若在开始监听之前就死掉（开不了的数据库、占不到的端口），除了窗口那句「连不上运行时」什么都不会留下。

### Messenger

- 新建 Bot 的生成头像跟着你打的名字走。以前创建弹窗会把空名字那一版锁死，不点「换一个」每个新 Bot 都是同一张脸。现在打名字就会重画；上传的图片不受影响。
- 拖过的会话列表宽度、产物预览分栏宽度和预览里的文件树宽度，退出后再开还在。这些宽度本来就会写进 `localStorage`，但加载时按偏小的默认壳去钳，宽列表或宽文件树一重开就变窄。现在按记下的宽度还原；窗口变窄只临时收紧，变宽再回到记住的值。
- 预览面板里的视频和音频，不会再因为引用它的那条消息离开已加载的转录而从头开始——切换会话、或者点「继续」续跑中断的轮次时都不会了。面板原先按「那条附件是否还在已加载的转录里」来决定从哪儿取字节（在就走附件、不在就走工作区），这一翻转会重新取一次文件并把正在播放的元素的 object URL 换掉。现在每个文件按路径只加载一次；打开别的文件照常重新加载。

## 0.1.0-rc.4 — 2026-09-21

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Desktop

- **装出来的应用现在能起运行时了。**以前发布包根本没带守护进程：它按编译期烘进二进制的绝对路径去找 `apps/daemon/src/main.ts`（发布构建里烘的是 CI runner 的家目录），再去找一个用户根本没被要求安装的 `bun`。下载 `.dmg` 的机器上两样都不存在，于是窗口永远停在「连不上运行时」，对每个用户都必然复现，而且一句原因都不给。现在守护进程用 `bun build --compile` 编成独立可执行文件，作为 `externalBin` 随包放在窗口二进制旁边，装完即用，不需要 Bun、不需要 Node、不需要源码。源码运行方式不变（debug 构建仍优先走源码和 `--watch`），`REAL_BOT_DAEMON_MAIN` / `REAL_BOT_DAEMON_BIN` 可分别覆盖两种形态。自带运行时的代价是每个架构约 60MB。

### Daemon

- 卡住的轮次不再永远停在「思考中」——这最常发生在 Bot↔Bot 私聊里：没人盯着，那条永远不来的回复也就没人发现。轮次回路是脱离的，回路里抛出的异常会消失在被吞掉的 rejection 里，轮次一直留在 `running`，直到下次重启。现在抛错的轮次以「这一轮没写完：运行时出错」收尾；调度器每次 tick 还会扫一遍，20 分钟没有任何进展的轮次以「这一轮没写完：卡住了，很久没有任何进展」收尾。等待批准和等待回复的轮次不在扫描范围内；耗时长的壳命令与 MCP 调用会在两端各记一次活动，正经干活不会被打断。
- `shell` 单条命令超过 10 分钟就放弃并杀掉进程，超时作为工具失败返回。命令自己退出、但后台孙进程还攥着 stdout 管道的情况也不再吊住轮次——输出改成与超时赛跑，不再读到底。
- HTTP MCP 调用在响应流静默 5 分钟后放弃，读响应体期间你的 Stop 也能真正打断它。此前请求超时只管响应头，且响应头一到就把 abort 监听摘掉，于是「流开了但一直不说话」会把这次调用连同它背后的轮次永远吊住。工具查找或握手抛错现在也是一次失败的工具调用，不再把异常扔进轮次回路。

### Messenger

- 在 Bot 设置抽屉里选中文字、拖到抽屉外再松手，不再把抽屉关掉。以前只要 `click` 的目标是遮罩就关闭，而按下在抽屉内、松开落在遮罩上时浏览器正是这么报的——于是复制一个字段就把正在看的抽屉关了。现在遮罩记住按下的位置，只有按下也在抽屉外才关闭；真正点在外部依然会关。按下在捕获阶段记录，抽屉里拦掉 `mousedown` 的自定义控件也不会让这个判断失效。同一处理也覆盖设置弹窗、端点编辑器、Bot/群创建面板、MCP 与技能编辑器、工作区浏览器、模型选择日志和危险确认框。
- 支持右键点击消息时选中该消息并呼出右键快捷菜单，包含引用回复（填充至输入框）、复制消息内容（或局部选中文本）、打开关联的文件树（在工件预览分栏中展开该条消息关联的文件树）、复制消息 ID 以及快捷表情回应。
- Bot 设置面板改用带分类导航的布局，分为「基础信息」、「技能」、「记忆」和「操作」四个 Tab，解决内容过多一滚到底的问题。分类项支持未填错误角标与技能数量统计，Tab 切换时立即保存待写入改动，抽屉宽度微调至 460px。分类导航栏常驻吸顶，且基础信息中的职责与边界文本域高度扩容，长说明无需频繁纵向滚动。
- 设置不再有保存按钮。工作区选中即写入；端点字段和模型名单改完即存（文本停手片刻，勾选 / 点选随即）；关浮层或关设置前会把未发出的草稿送出去。新增 MCP 或改 command / args / URL / headers 仍须确认，没有 Always allow；名称和用法备注改完即写入。
- 聊天里文字很多的 Markdown 表格不再把短中文列挤成一字一行。表头保持单行，长单元格在可读宽度换行，超出气泡的部分横向滚动。
- 从 Bot 抽屉编辑记忆时弹窗会真正出现。以前表单和遮罩是平级的，模糊层盖住对话框，看起来像点了没反应。

## 0.1.0-rc.3 — 2026-09-20

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Daemon

- 清空历史或删除群不再在该会话已有模型选择复盘时静默失败。`route_reviews` 指向轮次和会话，这些行以前没删，外键把整段事务滚回去，信使把 500 当成断线——确认框关了，群和消息还在。该会话上的复盘随历史一起去掉。
- `read_file`（以及除 `write_file` 以外的其他工作区工具）不再把路径记成消息附件。Bot 只读过的路径是输入；只有写出来的才成为这条消息上的产物。
- **Bot 能跨会话记住事情了。** Bot 用 `remember` 记下一条事实，之后任何会话的每一轮都会读到——你在私聊里说的口径，第二天在群里依然生效。记忆归单个 Bot：别的 Bot 看不到，删 Bot 一并删掉，归档保留。同一个 subject 再记一次是覆盖而不是并列第二条，这也是 Bot 纠正自己的方式；不再成立就用 `forget` 删掉。记满 20 条会直接报错并点名最久没更新的那条，让它自己取舍，而不是有东西在你背后消失。没有检索、没有后台蒸馏器、不增加任何模型调用：启用的全部渲染进去，边界是从上限推出来的预算。
- 系统指令不再告诉 Bot「这里没有记忆层」，并且把三者的分工在一处说清：人设是你是谁，技能是怎么做，记忆是你学到了什么。记忆是以前的结论，不是事实来源——和本轮转录冲突时以转录为准。

### Messenger

- 远控未知结果会在断线后仍保留 `{id, method, path, fingerprint}`，包括运输层直接抛出 `ApiError(503, request_unknown)` 的情况。重连把同一条内存回执装回新的 RemoteApi，先查 `/v1/requests/:id`；聊天发送也不丢这个 id。聊天和 Markdown 的 SVG 缩略图与产物预览共用同一套清理（含 `<svg/onload>`、未闭合 `<script>`、无引号 `javascript:`，以及 SMIL `values` / `to` / `from` / `begin` / `end` 里的 `javascript:` / `data:` / `vbscript:`）。托管/远控设置与向导里工作区路径只读、跳过选目录步骤，PATCH 不再带 `workspace_path`；本机窗口仍用系统文件夹选择器。
- 凭据修复失败后再取消，会释放整条已被替代的内存请求链，不再让含旧密钥的过期载荷阻塞下一次新建/编辑。结果未知时保留待确认状态，直到显式恢复得到确认；不会自动重放副作用。
- 待确认变更只在内存保留逻辑请求 ID 和原始载荷，手动重试不再重复创建端点。设置提供手动重试、仅修复凭据、取消清除控件，不再把钥匙串失败当作运行时断连；关页不会排队或自动重放。
- 文件保存携带读取时的 ETag。Bot 或其它程序改过文件时，不再静默覆盖：保留尚未保存的编辑，并提示先复制编辑再重新打开文件。
- 产物预览里的 Markdown 把工作区图片和 SVG 链接画成缩略图，点一下仍打开预览；加载失败则保留原来的链接。同一套渲染里的表格铺满栏宽，长单元格换行，不再横向撑出去。
- 侧栏会话右键「删除」或「清除历史」会弹出针对这一行的确认框。以前会先跳进那条对话，又因为会话抽屉没开着，确认框随即消失。
- 「模型选择记录」可以筛选了：搜索 Bot、模型、理由、复盘和反馈；按结果、Bot、模型、消息类别多选；两个开关分别只看有模型反馈的轮次、以及复盘归咎模型的轮次。筛着的时候副标题变成「可见 / 全部」轮；清空筛选回到完整列表。筛选只活在这次打开的浮层里。
- Bot 设置抽屉在技能下面多了一张「记忆」卡片：它记住了什么、什么时候记的、来自哪个会话——来自 Bot↔Bot 私聊的会标出来，因为那场对话你不在。点来源跳回触发它的那条消息。你可以改、停用或删除一条记忆，但不能新建，因为写的人是 Bot。停用会把它移出提示词，同时留着记录让你看见 Bot 曾经得出过什么结论。

### Docs

- README、landing 页和路线图补上「模型到底怎么挑的」——这块的描述早就掉队了。路线图里还写着规则评分、按关键词收负反馈、干净轮次逐步抵扣惩罚是已有基础，把逐次反思列在正在建设，而 ADR 0019 在 0.1.0-rc.2 就把这些全换掉了。现在实际发出去的是：开轮前由 agent 挑模型和思考等级并留下一句理由，纠正链结束时复盘一次，只有判成模型不行的结论才留成这个 Bot 的经验。留下的是结论不是分数——没有权重、抵扣和封顶，词表里本来就这么写。还没做的部分也收敛到实情：工具和协作方式不是开轮前定的，执行结果没有核验，也还没有任何度量能说明复用经验是不是真的选得更准、完成更快。

### Daemon

- Bot↔Bot 私聊改成一次发起一条对话，不再是每对 Bot 一条长期线程。`create_direct` 每次调用新开一条会话，并记下来源——叫醒发起方那一轮的那条消息；同一轮里对同一个 Bot 再调一次仍返回这一条，不会重复。会话上多了 `origin_session_id` 和 `origin_message_id`，随 `GET /v1/sessions` 和 `session.upsert` 一起下发。
- **Bot↔Bot 私聊你只能看，不能发言。** 往里发消息返回 403 `not_a_member`；标记已读、归档、清空历史、Stop、放行审批都照常可用。已有的 Bot↔Bot 私聊升级后也变成只读。Bot 在这类私聊里调 `ask_user` 会被拒掉，不再把轮次停在一个没人能答的提问上。
- 私聊里没有用户在场时，对方第二条消息改道进现有活轮，而不是再分叉一轮——和群里对 Bot 的做法一致。
- `list_sessions` 只返回最近动过的 30 条并带上 `updated_at`，免得同一对 Bot 的一堆私聊变成分不清的记录白烧上下文窗。

### Messenger

- 作曲栏芯片和聊天 markdown 的 `@` 解析与守护进程同一套：最长名册名、唯一前缀或后缀、CJK 标点切词，邮箱和 `@scope/pkg` 当普通文本。名册里加一个名字或工作区多一种后缀，不必再在信使里抄一份规则。
- 产物预览里的 Markdown 和聊天气泡共用同一套渲染（`MarkdownBody`）：表格保持列对齐，有边框、表头底色，比面板宽时横向滚动；代码块、@提及和链接也跟 Bot 消息一致。
- 设置、会话抽屉和工作区浮层刷新后也会回来，和已经记在 URL 里的会话、产物预览同一套：共用 `?o=`（`settings`、`session`、`bot` 另加 `?b=<id>`、`workspace` 选中文件时另加 `?w=<relpath>`）。关掉就从地址里拿掉；会话、Bot 或工作区路径已经不在了则当没打开。模型选择记录、新建弹窗和确认框仍然不进 URL。
- Bot↔Bot 私聊的入口挪到了开起它的那条消息下方：卡片直接挂在那条消息底下，点开就是这条私聊。同一条消息开出的多条私聊并成一张卡片。
- Bot↔Bot 私聊里输入框锁上，并写明你不是参与者；引用回复、回答提问的输入框、表情回应的点击也一并收起，审批仍然可以放行。
- 侧栏 `Bot ↔ Bot` 只列最近活跃的几条，其余收在展开按钮后面。每行下面标出来源，点一下跳回触发的那条消息。这类行不再显示未读红点——你没法回复它消掉。排序看最后一条消息而不是 `updated_at`，所以点开看一眼不会再把它顶到最前面。
- 发送输入不再铺一条通栏雾面底：毛玻璃只紧贴输入区轮廓——建议芯片贴在输入框左肩上，单行按芯片内容撑开（不定宽、不换行），和输入卡片连成一块「烟斗」形；快捷键提示单独一圈。宽屏两侧对话内容直接露出来。
- 检查到新版时，「关于」卡片直接列出这一版改了什么：检查结果带上 release 正文（也就是该版本的 CHANGELOG 段落），卡片按 `###` 分组画成条目列表，跳浏览器的「查看发布说明」留着但不再是唯一入口。列表只取标题和条目，段落和围栏命令留在发布页；正文不走聊天的 markdown 渲染器，否则 changelog 里的路径会被当成工作区产物链接。
- 重构 Bot 设置面板中的「技能」（Skills）交互与视觉设计：
  - 彻底解决面板空间挤占：将新增与编辑技能由原先卡片内联展开（展开达数百像素撑爆侧边栏）升级为专属的居中模态对话框（Skill Modal，宽 520px，支持快捷键 Escape 关闭、遮罩点击关闭），提供充裕舒适的 Markdown 编写空间，卡片本身始终保持紧凑整洁。
  - 操作按钮一目了然：技能卡片头部右上角常驻显式的「+ 添加技能」操作按钮；列表每一项右侧配备清晰明确的启用开关、编辑（铅笔图标）与删除（垃圾桶图标）操作按钮，并支持点击技能信息块直接打开编辑；空状态优化为带引导文案与快捷添加按钮的轻量卡片。

### Desktop

- 「关于」里的更新卡片跟着应用语言走。一个 release 只有一份正文而应用有两种语言，所以正文两种都带：`release-notes.ts` 从 `CHANGELOG.md` 和 `CHANGELOG.zh.md` 各取该版本那一段，用 HTML 注释分段标好——注释在 release 页面上看不见，那边就是两段叠着显示。卡片按 `settings.locale` 挑对应那段，没翻译过的语言落回英文，末尾公共部分两种语言都带上。标记出现之前发布的正文（rc.2 及更早）没有标记，整段原样渲染，和以前一模一样。缺翻译不挡发布；`CHANGELOG.md` 里缺该版本仍然挡。
- release 正文改成自动从 `CHANGELOG.md` 里取该版本那一段（`apps/desktop/scripts/release-notes.ts`），不再是一句「去看 CHANGELOG 和 ROADMAP」；`check_for_update` 把正文随检查结果一起回给信使。CHANGELOG 里没有该版本的段落时打包任务直接失败，免得发出一个空的更新说明。

## 0.1.0-rc.2 — 2026-09-19

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Daemon

- 本机接口同时听 `127.0.0.1:17890` 和 `[::1]:17890`，Origin 白名单补上 `http://[::1]`，CORS 回显 `Access-Control-Allow-Private-Network`。Vite 开发页常开在 `http://[::1]:5173`；此前只听 IPv4 且把 IPv6 Origin 判成禁止，健康检查失败，信使只显示「连不上运行时」。`localhost` 与 `127.0.0.1` 不变。
- 选模型和评判选得对不对都交给 agent，不再是正则加权重（见 [ADR 0019](docs/adr/0019-agent-routing-and-review.md)）。**开轮前**发一次短调用：看这条消息、Bot 人设、候选名单和这个 Bot 最近的复盘结论，答出模型、思考等级和一句理由；它固定跑在默认端点的默认模型上（避免递归），答不出、超时或答了不存在的模型 / 档位就落回规则兜底，不重试、不卡用户；Bot 钉死了模型和思考等级时跳过。**纠正链结束时**再发一次复盘调用：看整段——触发消息、选了什么、Bot 的回复、之后用户说的每一句、这轮怎么结束——答出是 `model`（模型不行）、`task`（事情本身难）、`prompt`（需求没说清）还是 `none`，外加方向（stronger / lighter / faster / cheaper）、纠正轮数和把握度。链的边界由同一次选路调用顺带判定（这条消息是不是还在说上一件事），加上静默 3 分钟兜底；静默定时器只在内存里，所以守护进程启动时会扫一遍上次没来得及复盘的链（只看最近 24 小时内、一次最多 20 条）。
- 用户的跟进消息不再按关键词筛：每一句都原样留给复盘。此前「这里不对」「还是不行」一个都匹配不上，而「@导演 你 @ 的分镜不对」匹配上了，让九个 Bot 各挨一分、把 `grok-4.6` 永久排除在外。只有复盘判成 `model` 且把握度够才留成经验。
- 留下的是结论不是分数：新表 `route_reviews`（fault / direction / rounds / confidence / reason），下次选路原样读最近几条。`route_learned`（按权重加减的惩罚表）、`isCritiqueMessage`（正则）、`PENALTY_WEIGHT` / `CRITIQUE_WEIGHT` / `FAILURE_WEIGHT` / `POSITIVE_RELIEF` / `PENALTY_CAP` 一并删除；擅长领域、价格、档位轻重降级成选路不可用时的兜底。
- `turn_route_decisions` 补上 `reason`（选路理由）和 `chain_id`（同一条纠正链的多轮共用）。`GET /v1/sessions/:id/routes` 除 `items` 外多返回 `reviews`。
- 新增 `GET /v1/sessions/:id/composer-suggestions`：按当前会话最近转录做一次短、无工具调用，返回最多 4 条用户下一步可发送的草稿（芯片短句 + 完整正文）。点名只保留在场成员或 `@everyone`；编造的 `@` 丢弃。没配默认端点、调用失败或返回不可信时返回空列表，不插转录、不挡输入。短调用打默认端点上名字带 flash / mini / lite / fast 的模型（没有就用默认模型），8 秒超时。
- 思考等级不再能脱离模型单独钉。要么两个都不钉（模型和等级都由应用每条消息挑），要么钉一个模型并同时定下等级：没钉模型却传 `thinking_level` 是 `422 thinking_level needs a pinned model`；钉了模型而没给等级会落到该模型的默认档（应用对普通消息的偏好顺序）；换成不支持原档的模型落到新模型的默认档；清掉模型一并清掉等级。`update_profile` / `create_bot` 的说明和系统指令同步。旧库打开时把半钉的 Bot 补齐：只有等级的清掉等级，只有模型的补上该模型的默认档。
- 思考等级不再锁死为 none / low / medium / high。`POST /v1/models/probe` 除名字列表外还返回 `catalog`：端点 `/models` 对象若带了 `reasoning_efforts` / `thinking_levels` / `reasoning.supported_efforts` 等字段，就把那些名字（如 Grok 的 `xhigh`、Gemini 的 `max`）写进该模型的 `thinking_levels`，补全原样发给 `reasoning_effort`。名单没写则仍用原来的四档。Bot 钉档、工具参数和路由打分都认这些名字；没钉时应用按档位轻重挑（闲聊偏轻、推理偏最重）。设置里仍可手改或加自定义档。
- 每轮的模型选择经验改为按 Bot 保存（新表 `route_learned`，主键 Bot + 消息类别 + 模型 + 思考等级），选模型只看该 Bot 自己的经验；对 Reviewer 说「换个模型」不再改变 Writer 的选择。升级时旧的名册级 `route_learned` 设置项复制给当时在册的每个 Bot 各一份后删除，升级当天没有 Bot 改选；删除 Bot 一并删掉它的经验。
- 路由反馈按对象归属：引用回复记在被引那一轮上，`@` 了唯一一个 Bot 记在它最近那一轮上，否则记在会话里最近可见的那一轮上（此前只看最近一条触发消息，群里常记到别人头上）。
- 每轮的模型选择记录（`turn_route_decisions`）补上 `bot_id`、`provider_id`，并在轮次结束时写 `outcome`（completed / failed / stopped / redirected / interrupted）与失败种类。端点拒绝或回复不完整记半分负面经验；连不上、忙、5xx、流卡住只记录不学习；干净完成记一分正面，按 0.25 抵扣负面；惩罚封顶 3。旧库打开时自动补列并回填。
- 打开旧库时把 `outcome` 为空的模型选择记录按其轮次的终态回填（completed / redirected / interrupted / stopped，`finished_at` 取轮次的更新时间），进行中的轮次不动。此前补上 `outcome` 列时没有回填，早先的记录在信使里会一律显示成「进行中」。
- 新增 `GET /v1/sessions/:id/routes`：按时间列出该会话每轮选的模型、思考等级、消息类别、所属 Bot 与端点、结束方式和收到的反馈。
- 守护进程内部把 `store.ts`（3400 余行）拆成 `store/` 下按领域划分的模块（settings、providers、bots、sessions、messages、turns、approvals、routing 等），`prompts.ts` 拆成 `prompts/`（系统指令、判断指令、转录文案、按域分组的内置工具定义）。`Store` 对外方法与本机接口不变。
- 全局搜索 `GET /v1/search` 返回的 Bot 结果增加 `avatar` 字段，搜索结果带上 Bot 头像。
- 端点多一个 `available_models`：端点 `/models` 上次返回的完整名单（`providers.available_models` 列，`POST/PATCH /v1/providers` 同名字段，`GET /v1/providers` 与 `provider.upsert` 一并返回；非字符串数组 `422 available_models must be an array of strings`）。它只是给信使的挑选列表，不影响启用名单 `models`、默认模型和路由；`list_endpoints` 工具也带上它。旧库打开时自动补列，缺省为空。
- 没钉端点的 Bot 只在默认端点的模型名单里挑模型，设置里新加的端点不再自动进所有 Bot 的候选；要用别的端点必须在 Bot 上显式钉端点，钉的模型名只在别的端点上时照钉的用。此前加了第二个端点后，一条学习到的惩罚就足以把未钉的 Bot 送去那个端点。
- 路由反馈只认说到模型本身的跟进消息（选的模型、换个模型、太慢、太浅、太贵、幻觉等）；「不对」「有问题」「重来」这类对回复内容的批评不再记成对上一轮模型选择的惩罚。
- Bot 可以钉思考等级。`bots.thinking_level`（补全的 `reasoning_effort`，档名以该模型名单为准）随 `POST /v1/bots`、`PATCH /v1/bots/:id` 和 `bot.upsert` 一起走；`null` 仍由应用按每条消息挑。开轮时所选模型支持这一档就用它，不支持则照旧由应用选。钉了模型时该档必须在该模型名单的 `thinking_levels` 里，否则 `422 thinking_level must be one the pinned model supports`；只换模型而新模型不支持原来那档会自动清掉。`update_profile` / `create_bot` 多一个 `thinking_level`（null 或空字符串清掉），`list_bots` 一并返回；系统指令写明用它改自己的思考等级。

### Messenger

- 新建群改成和新建 Bot 一样的居中弹窗，不再是只盖侧栏的滑出。成员不再是一长串复选框，而是可搜索的多选下拉：每行是 Bot 头像 + 名字 + 职责（没有图片的用名字首字，配色和名册一致），按名字或职责过滤；挑中的成为字段里的一枚带头像的芯片（点芯片的 ✕、或在空输入上按 Backspace 移出），挑完之前列表不关；Escape 先关列表，弹窗留着。
- Bot 设置把原先分开的「头像」和「人设」并成一张「Bot 基础信息」卡片；归档单独放进「会话操作」，清空历史和删除对齐群组设置的「危险区域」行（标题、说明、右侧按钮），不再挤在同一排红框按钮里。
- 输入框与上方自动提示选项区域采用半透明雾面玻璃（`--glass-composer` + `backdrop-filter: blur(16px)`）底栏，对话内容滚动穿过底部时自然模糊消解，消除内容文字与建议芯片、输入框视觉交叠混淆；内部输入框使用 `var(--input-bg)` 实体底色保持高对比度与清晰输入焦点；「回到底部」浮动按钮调整至底栏上方安全间距。
- 聊天里代码块的复制按钮补回图标：那段 SVG 的圆角弧线少写了一个半径，浏览器会整条路径丢弃，按钮上只剩半个方框。
- 打开的是哪个会话现在记在 URL 的 `?s=<id>` 上，打开的产物预览记在 `?p=<relpath>` 上：刷新、开发态热更新和窗口重载都回到刚才那个会话和预览，浏览器的后退 / 前进在会话之间走，关预览也进历史。链接指向已经删掉的会话时退回未选状态，不报错；预览路径非法（空、绝对路径、逃出工作区）则当没打开。用查询参数而不是路径，是因为打包后的窗口直接服务静态文件，没有 SPA 回退，路径形式一刷新就 404。设置、抽屉、模型选择记录和工作区浮层仍然不进 URL。
- 对话正文不再随窗口拉满：消息流与输入框共用同一条居中栏（最大约 800px），宽屏两侧留白，行宽接近 Gemini / Grok / DeepSeek 一类聊天页。顶栏、侧栏和滚动条仍铺满主栏；窄屏仍用满可用宽度。
- 浏览器开发态发现本机接口时，按当前页面的回环地址族拼 origin：`http://[::1]:5173` 连 `http://[::1]:17890`，`localhost` 仍连 `127.0.0.1`。Chrome 把跨地址族的回环请求当成本地网络访问拦掉，否则会一直显示「连不上运行时」。
- 「模型选择记录」每行多两样东西：agent 选它时给的那句理由，以及这条纠正链的复盘结论（判成谁的问题、该往哪个方向调、用户改了几轮、一句说明）。判成模型问题的结论用警示色标出来——只有这一种会改变以后的选择。
- 群聊输入框上方不再铺一排静态 `@everyone` / `@Bot` 芯片。改为根据当前对话建议下一步要发的草稿（需要叫醒谁时草稿里仍可带 `@`）；点芯片填进输入框，再按 Enter 发送。输入框里打 `@` 的补全列表不变。
- 设置分类侧边栏拆出「基础偏好」与「关于」为独立分类：左侧导航栏在一级分类中新增「基础偏好」（外观主题、语言、登录启动）和「关于」（版本信息、检查更新与更新下载），原「通用」分类专注保留工作区目录配置与安全边界说明；在有未忽略新版本时，「关于」分类按钮同步显示红点提示。
- Bot 面板和新建 Bot 里的「模型」与「思考等级」变成一组：模型选「自动」时不再单独列思考等级（两个都由应用每条消息挑），选了具体模型才出现思考等级，且只列**这个模型**支持的档——此前那排固定档位是所有模型的并集，会让你在 Grok 上挑到只有 Gemini 有的 `max`。切换模型时保留仍被支持的档，否则落到新模型的默认档，不会停在「选了模型没选等级」的半截状态。
- 输入框改为浮在对话内容底部：外层容器采用透明背景悬浮于消息流之上，输入卡片采用半透毛玻璃与悬浮投影，位置不再紧贴窗口最下边缘；对话消息流延伸至窗口底部，内容滚动时自然从输入框后方穿行，底部留足呼吸间距，最新消息不被遮挡；「回到底部」悬浮按钮移至窗口右下角独立展示（位于居中输入框右侧区域），不再叠在输入框上方。
- 重构设置面板「通用」（General）设置内容布局：将工作区目录、基础偏好与关于信息重构为卡片化视觉分组（配备图标徽标、次级说明与状态指示）；工作区卡片集成路径展示、更换目录入口与专属安全边界说明浮层；基础偏好卡片采用结构化行布局，外观主题与显示语言升级为原生分段胶囊控制（Segmented Control，带主题图标），系统登录启动升级为开关（Toggle Switch）；关于卡片整合版本号胶囊与更新检查状态操作栏，全面适配明暗主题及窄屏/移动端自适应折叠排版。
- 「回到底部」按钮贴到对话流右下角（输入框正上方），不再悬在半空；点了会滚到真正的最后一行，中途不再停住。
- 会话顶栏多一颗「模型选择记录」：点了从右侧滑出独立浮层（自己滚动，Escape 或点遮罩关掉），按时间倒序列出这个会话每轮开轮时挑的模型和思考等级、这条消息算哪类、是哪个 Bot、这轮用了多久、怎么结束（补全失败带失败原因），以及后来落在这一轮上的模型反馈（点开才展开正文）；配了多个端点时还标出这轮打到哪个端点。点一行跳回触发消息并高亮。不放进当前对话设置：一个跑了几百轮的群会把设置面板撑得看不见「会话操作」和「危险区域」。长列表只把看得见的那些行放进 DOM（上下各多留几行，其余用等高的空位顶着，行高按实际测量修正），几百轮的会话从几千个节点降到两百上下。数据走 `GET /v1/sessions/:id/routes`：打开时拉一次（不再随每次切会话预拉），开着时该会话每次轮次状态变化再拉一次（没有推送事件）。这里不显示 token 或花费。
- 重构群聊设置（Group Settings）面板布局：扩展面板宽度至 420px 提供舒适排版空间；顶部群名片卡整合复合头像、成员统计徽章与群名修改行（未修改置灰、修改后高亮并支持 Enter 键直接保存）；群成员列表重新排版，创建者展示「创建者/Owner」徽标，Bot 成员的名字与模型标签同排芯片化对齐，彻底消除此前文字重叠冲突，并提供职责描述与轻量化移出按钮；将原先混杂挤压在单行的底部按钮彻底拆分为独立的「会话操作」卡片（群组归档/移出归档）与专属的「危险区域」卡片（清空聊天记录、删除解散群聊），每个操作配有明确影响说明与单行操作按钮，消除按钮文字折行与操作混淆。
- 修复产物编辑器打开查找后语法高亮被语言配置冲掉；查找栏按钮悬停提示不再挡住按钮、也不再闪烁。
- 设置「通用」和工作区向导里的工作区不再是可编辑输入框：路径只展示，点「选择文件夹 / 更换文件夹」打开系统文件夹对话框；浏览器开发态打不开系统对话框。向导仍可一键使用推荐目录 `~/real-bot-workspace`。
- 侧栏底部工作区（⌘O）改为从右侧滑出的独立浮层：左侧完整工作区树、右侧自己的预览/编辑器，面板更宽，背后有遮罩。对话框旁那条产物预览只给聊天里点开的内容用，两者互不占用。Escape 先关工作区浮层。
- 设置面板重构为左右分栏布局并放大尺寸：左侧常驻分类导航侧边栏（通用、模型服务、MCP 扩展，带图标、数量徽标及未配置警告），右侧为主内容区与操作底栏；整体尺寸扩至 880×640px，内容展示与端点列表更加开阔舒展；支持移动端及窄屏自适应响应式折叠。
- 产物预览和工作区文本编辑器补上查找、替换和匹配段落折叠。Cmd+F 打开查找（工具条也有「查找」），Cmd+G / Shift+Cmd+G 下一个 / 上一个，Ctrl+G 跳行，Cmd+⌥F 替换；行号旁可折叠匹配的括号、HTML/XML 标签、Markdown 标题与围栏、`#region` 以及缩进块。Escape 先关查找再关预览。
- 全局搜索结果对 Bot 和会话展示头像（Bot 显示其自身头像或名字首字底色徽标，会话显示包含多成员拼贴与溢出计数器的会话头像）；搜索框失去焦点（点击外部或失焦）时自动收起结果下拉浮层，再次聚焦搜索框且有搜索内容时重新展示；支持键盘方向键（↑ / ↓）在结果列表中上下移动高亮选中项，超出可视区自动平滑滚动，按 Enter 键直接跳转至当前选中的搜索结果项。
- 输入法选词时按 Enter 只确认候选，不再把未上屏的拼音或半截词发出去。上屏后浏览器多打的那一下 Enter 也会吞掉；词已经在输入框里之后再按 Enter 才发送。
- 设置面板「模型服务」里的端点卡片重新设计：顶部结构化展示端点图标与首字母标记、端点名称、默认端点徽标（★ 默认 / ★ Default）、密钥配置状态指示灯（● 已配置 / ○ 未配置）以及端点 Host 域名；右上角平铺「设为默认」「编辑」「删除」操作按钮（删除带确认浮层）；卡片主体展示默认模型标签（含高亮标识与闪电图标）、可用模型总数及前排模型芯片预览，支持点击主体快速呼出编辑，卡片外观与明暗主题、桌面及移动端视口自适应对齐。
- 端点编辑浮层重做。填好端点 URL 和密钥停手约 0.7 秒就自动获取一次模型列表（同一对 URL + 密钥不重复拉，也可手动「重新获取」）；拉到的完整名单随端点保存，再进编辑时直接展示，不用再拉，旧端点名单为空时打开会自动拉一次。模型不再是「每行一个」的多行文本框，而是勾选列表：超过 6 条出现搜索、「全部 / 已启用」切换和全选 / 清空；名单里没有的名字从底部「手动添加模型名」补进去；什么都没勾且名单不超过 3 条时整单直接启用，更长的名单等你挑。每个已启用模型的决策属性收进该行右侧的折叠里：价格是数字框，思考等级和擅长领域都是点选芯片（擅长可加自定义标签），思考等级至少保留一档；端点广告了 `xhigh` / `max` 这类档会自动勾上，也可再手动加。默认模型从已启用的名字里选。向导里拉到的名单也会随第一个端点存下来。
- 设置「模型服务」里删除端点改到卡片上，点了弹出确认，不再放在编辑浮层里。
- 产物预览里点「源码」会切到 Monaco 编辑器，不再被空 iframe 挡住；切文件才退出源码，会话刷新不会把正在看的源码打回预览。Monaco 还没起来时先显示纯文本。HTML 源码按 Shiki 着色（含内嵌 CSS / JS）：开发态不再把巨大的 HTML 语法打进过期的 Vite 预构建块，Monaco 也会先注册 `html` 再挂分词器。源码着色用 `vitesse-light` / `vitesse-dark`，跟随应用明暗；聊天围栏代码块继续用 github 主题。HTML 预览会写入 `color-scheme`，随外观切换。
- 单文件 HTML 预览 iframe 允许脚本（`allow-scripts`，不含 `allow-same-origin`），CSS 动画和内联 JS 动效能在侧栏播；外链脚本仍受窗口 CSP 限制，和在 Chrome 里打开不是同一套网络权限。
- Bot 面板不再有「保存」「关闭」按钮：所有改动自动保存。名字 / 职责 / 边界停手约 0.6 秒后 PATCH，头像 / 模型 / 思考等级点了就存；切换 Bot、返回群设置或关面板前先把未保存的发出去。人设卡片头显示「保存中… / 已自动保存」，字段错误仍画在字段下。Bot 自己改人设推来的 `bot.upsert` 只在你没有未保存改动时刷新，自动保存回显的修剪空白不会吞掉你正在输入的尾部空格。
- 移除从群聊设置进入 Bot 详情时顶部冗余的头像与名字横幅（与下方头像编辑卡片和人设卡片内容重复），使人设配置在滑出面板中直接居顶呈现。
- Bot 面板和新建 Bot 滑出多一排「思考等级」快捷档位：「自动」加上所选模型支持的档；换成不支持当前档的模型会自动退回「自动」。

### Desktop

- 新增 `pick_workspace_folder`：打开系统文件夹对话框（可新建文件夹），把选中的绝对路径回给信使。
- 窗口 CSP 允许 HTML 预览 iframe 跑内联脚本和样式（`script-src` / `style-src` 的 `'unsafe-inline'`，以及 `script-src-attr`），外链和本机接口范围不变。

## 0.1.0-rc.1 — 2026-09-18

未签名的 macOS rc。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Daemon

- 启动时先占本机接口端口，抢不到就不打开状态库、也不把别人的活轮标成中断。开发态 `--watch` 热重载或健康检查超时再拉起一份时，不再把群里正在跑的轮次误标成「中断」。健康在 socket 就绪后立刻可答，避免窗把启动空档当成守护进程已死。
- `GET /v1/workspace/file` 不再因超过 1MB 拒绝，预览栏可以打开区内视频等较大文件。1MB 上限仍只约束 `PUT /v1/workspace/file` 覆盖 UTF-8 文本。
- 系统指令写清技能与 MCP 的选用顺序：技能是工序，MCP 是能力。先看技能目录，命中就 `read_skill` 照做，正文点到的 MCP 工具按 tools 数组里的名字调用；没有命中的技能才直接按「本轮 MCP」段挑工具。技能不新增工具，也不替代 MCP；「技能」段和「本轮 MCP」段的引言同步说明这条顺序。
- MCP 服务器多一条名册级用法备注 `usage_note`（做什么、何时用、何时不用），进每一跳的「本轮 MCP」段并排在服务器自带 `instructions` 前面；备注优先于服务器说明，留空则只渲染服务器说明。`add_mcp_server` / `update_mcp_server` 可带 `usage_note`，改备注直接执行、不等批准，改 command·args / url / headers 也不清掉它；空字符串清掉，上限 2000 字。本机接口 `POST/PATCH /v1/mcp-servers` 同一字段。
- 技能可声明依赖的 MCP 服务器名 `uses`（`create_skill` / `update_skill` 与 `POST/PATCH /v1/skills` 同一字段，最多 16 个，按名字不分大小写去重）。技能目录里每条技能下多一行「依赖 MCP：…」，本轮未连接的标「（本轮未连接）」并提示正文照做不了时直说或用 `ask_user`，不拿别的工具凑。
- `read_skill` 会把正文里写的、但不在本轮 tools 数组里的 `mcp_` 工具名列在 `stale_tool_names` 里并附提示：服务器改名、停用或撞名后缀变了都会让硬编码的名字失效，Bot 据此按「本轮 MCP」段找现名并用 `update_skill` 改正文，而不是调用时才报错。
- 新增工具选择评估：`pnpm --filter @real-bot/daemon eval:tool-selection --base-url … --model …`（密钥走环境变量 `REAL_BOT_EVAL_API_KEY`）。用真实轮次同一套 system（技能目录 + 本轮 MCP）和 tools 数组，对 `apps/daemon/eval/tool-selection-cases.json` 里的每条触发消息各打一次补全，只看第一个工具调用是否命中期望（读对技能 / 选对 MCP 工具 / 用内建工具 / 直接回复），并检查禁止调用的工具和编造的工具名。按模型、类别、语言汇总命中率，结果写到 `.scratch/tool-selection-eval/`。自带 16 条中英文用例，覆盖技能命中、相近技能干扰、MCP 直选、用法备注区分同名服务器与只读约束、内建文件工具和纯聊天。
- Bot 可以有自己的技能：具名工序（何时用 + 怎么干），挂在该 Bot 上，不是名册共享、不是工作区 skill 包。启用中的技能以目录进入每一跳 system；正文用 `read_skill` 按需读进本轮。Bot 用 `create_skill` / `update_skill` / `delete_skill` 自己维护，改完不插转录条。本机接口 `GET/POST /v1/skills`、`PATCH/DELETE /v1/skills/:id`。
- 中断行可点「继续」：`POST /v1/turns/continue` 以该条系统消息为触发条新开一轮，不重试断掉的工具；下一轮仍只看一次「上次断了」。
- Bot 点名写错名字不再静默丢失。`@分镜` 这类缩写若只能对上一位在场成员（是其名字的前缀或后缀，至少两个字，忽略大小写），就按那位成员处理并叫醒；完全对不上时 `send_message` 先返回 `unknown_mention` 并列出在场成员的完整名字，让 Bot 在同一轮改正重发，再次仍对不上才发出，并在群里插一条系统提示「@xx 没有匹配到群成员。在场：…」。群轮的局面块新增「在场成员（点名请逐字写全名）」一行；系统提示与 `send_message` 说明要求点名逐字复制。未解析的 @ 按空白与中英文标点断词，不再把整句吞成一个名字。
- `@` 后带数字的词不再当成写错的点名。制片写「峰值 −1.0 dB @37.79s」曾被截成 `@37` 并在群里插一条「@37 没有匹配到群成员」；现在 `@37.79s`、`@f96`、`@t37`、`@14:30`、`@2026-09-18` 这类「在……」的写法既不插系统提示，`send_message` 也不再为它返回 `unknown_mention`。只有它恰好唯一缩写了一位在场成员（如 `@3D` 对「3D师」）时仍按那位成员处理。
- `send_message` 传 `parent_id` 是引用回复，留在主转录。引用 Bot 时正文自动加上 `@对方`（已有点名或 `@everyone` 则不重复），对方必须下场。Bot 可按情况选用，不是每条都要引用。

### Desktop

- 健康检查暂时失败时，若 `local-api.json` 里的 pid 还活着，窗口不再拉起第二份守护进程。避免开发态 `--watch` 热重载空档把群里的活轮标成中断。

### Messenger

- 聊天正文里的工作区路径（表格单元格、内联链接、反引号）点开后从工作区读字节预览。不再要求该路径已挂成该条消息的附件；没有附件记录时与工作区浏览同一条 `GET /v1/workspace/file`。
- MCP 编辑弹窗新增「用法备注」多行框：写给所有 Bot 看，排在服务器自带说明前面；只改备注不用确认直接 PATCH，留空则只用服务器自带说明。
- 人设抽屉的技能编辑器新增「依赖的 MCP」一栏：填正文要调的 MCP 服务器名，逗号分隔，可留空；保存进 `uses`。
- 人设抽屉可列表、编辑、停用和删除该 Bot 的技能；对话里 Bot 改了会即时刷新。
- 群消息里的 @ 与守护进程同一套解析：缩写能唯一对上成员时显示为该成员的完整名字芯片；对不上的 @ 用虚线下划线弱提示，悬停说明「这个 @ 没有匹配到群成员」。
- `@37.79s`、`@f96`、`@14:30` 这类 `@` 后带数字的写法按普通文字显示，不再加「没有匹配到群成员」的虚线下划线。
- 主转录消息悬停工具栏在复制旁增加「回复」。点了会在作曲栏挂上被引的那一条，发送带 `parent_id`；引用 Bot 时正文自动加上 `@对方`。气泡里显示引用条，点一下滚回原消息。引用回复留在主转录，不再藏进侧栏线程。
- 中断行和其他 Bot 消息一样带头像与名字，并可点「继续」：按钮贴在「中断」气泡后面，该 Bot 新开一轮从断处接着做，不重试断掉的那一下。

### Landing

- 设计理念不再整页铺开 CONTEXT.md：总览只保留前言和按主题的术语索引，术语拆到人与名册、会话、协作、工作区、运行时、模型与工具、批准与边界七页。文档区左侧是分组导航，右侧是本页目录，页脚可上一页 / 下一页；旧的 `/manifesto#term-…` 深链接会跳到对应主题页。

## 0.1.0-alpha.3 — 2026-09-17

未签名的 macOS alpha。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Daemon

- 保存工作区路径时，尚不存在的绝对目录会自动创建。相对路径和指向文件的路径仍拒绝。首次向导里点「使用推荐目录 (~/real-bot-workspace)」再完成设置，不再因目录未建好而退回第一步。
- 群里同一 Bot 同时最多一轮进行中：再被 `@` 或判断下场时听进那一轮，不再分身。私聊、日程和显式「另开一轮」仍可同时多轮。
- 群轮补全会看到一条只含事实的局面（谁有活轮、谁叫醒、用户最近一句），判断调用同样带上；达成一致仍由 Bot 自己停嘴，没有跳数或花费熔断。

### Desktop

- 窗口进程在启动 15 秒后、之后每 6 小时向 GitHub Releases 查询新版本；请求只走 Rust 侧的 `ureq`，结果在进程内缓存 30 分钟，手动点「检查更新」强制刷新。当前版本是预发布（如 `0.1.0-alpha.3`）时所有 release 都参与比较，是正式版则只看正式 release。可设 `REAL_BOT_UPDATE_FEED` 指向本地假源做验证。
- 新增打开发布页 / 对应芯片 `.dmg` 的命令，只放行 `https://github.com/Blackman99/real-bot/` 前缀的地址。

### Messenger

- 侧栏底部工具条收紧：设置按钮移到右侧与外观菜单并列，图标按钮改为无边框、悬停才显示边框与阴影，间距与角标随之缩小。打开设置时按钮保持选中态。
- 设置 → 通用 新增「关于」卡：显示当前版本、「检查更新」按钮；有新版本时可「下载更新」（对应芯片 `.dmg`）、「查看发布说明」、「忽略此版本」，未忽略的新版本会让侧栏设置齿轮显示小红点；忽略记录只存在本机，浏览器开发态（无 Tauri）不显示该卡。

### Landing

- 窄屏演示改为「聚焦缩放」：不再把整扇窗口缩到看不清，而是按每步标注的位置把镜头推到相关区域（设置里的密钥框、批准卡、右侧编辑器等），场景切换时平滑移动。
- 往回滚动时直接显示上一步的终态，不再从头重播；相邻步骤同时进入判定带时按离视口中心最近的一步切换。
- 进度条改成八个可点击的步骤块（最小 32px 高，当前步显示标题），步骤标题本身也可点击跳转。
- 900px 以下新增折叠菜单，包含全部导航项、语言切换与 GitHub；页脚补「下载」与「边界」入口。
- 首屏与下载卡片显示当前版本号（构建时读 `tauri.conf.json`）；`pnpm` 命令、Gatekeeper 放行命令和终端示例都有复制按钮；演示区说明补一句「窗口为示意重建」。
- 外观切换的选中态改由首屏脚本写入的 `data-theme-preference` 驱动，水合前不再闪一下；小字颜色加深到满足对比度要求；每步标注对读屏用户以隐藏文本提供；流式光标遵守减少动效设置。
- 设计理念与路线图页面加目录：宽屏在右侧粘性显示，窄屏为可展开的「目录」块；标题与 CONTEXT.md 的每个术语都有锚点，可直接深链接。

### Documentation

- CONTEXT.md 开头的 `**WIP：**` 改为 `**WIP**：`，让加粗能被 Markdown 正确解析。

## 0.1.0-alpha.2 — 2026-09-17

### Landing

- 站点所有「开发中 / WIP」措辞改为 Alpha。导航与首屏新增「下载」入口，指向最新 GitHub Release；「获取」区块加下载卡片（Apple 芯片 / Intel 两种未签名 .dmg 与 Gatekeeper 放行命令），从源码启动的说明保留。
- 品牌图页面新增 `variant=hero`：由群聊、待批准卡和产物编辑器三扇窗口组成的 1600×900 组合图，亮 / 暗两版，供 README 使用；OG 图与社交预览图按 Alpha 措辞重新生成。

### Messenger

- 聊天里带中文等非 ASCII 的工作区路径链接点开后，预览栏不再显示百分号编码路径，会打开原来的文件。

### Documentation

- README 重写为精简版：顶部是应用界面组合大图（亮 / 暗两版，随 GitHub 外观切换），其下只留一句定位、官网 / 下载 / 语言三个入口、四枚徽章，以及「它做什么」「获取」「状态」「参与」四段；中英文同步。新增 `docs/assets/`：README 组合图与 1280×640 的仓库社交预览图，均由落地页开发态的 `/og/[lang]` 品牌图页面截图生成。
- GitHub 仓库补充发现性 topics（ai-agents、multi-agent、llm、model-context-protocol、desktop-app、svelte、typescript、rust），移除 wip；描述改为 Alpha 措辞，主页保持指向落地页。社交预览图需在仓库设置里手动上传，步骤见开发说明。
- 默认 `README.md` 改为英文，中文说明移至 `README.zh.md`。

## 0.1.0-alpha.1 — 2026-09-17

未签名的 macOS alpha。不是受支持的签名安装包；Gatekeeper 可能拦截。优先从源码运行。

### Daemon

- 补全流中途卡住时，已写出的正文或完整工具调用会收下并继续这一轮，不再插入「这一轮没写完：回复中途没有下文了」。还没有可用输出时会自动再试；注释心跳会续上等待。同一端点同时最多两条流，群里多个 Bot 并行时其余排队，避免把端点打挂。

### Desktop

- 应用图标换成新的品牌标识：青绿圆角方块里两个叠放的队友头像（白与芥黄），源文件 `apps/desktop/src-tauri/icons/app-icon.svg`，用 Tauri CLI 重新生成了 `.icns` / `.ico` / 各尺寸 PNG；Dock、托盘与安装包都使用这套图标。

### Messenger

- 窗口 favicon 从 Svelte 占位标换成 Real Bot 标识。
- 作曲栏 `@` 点名候选超出列表高度时，方向键移动高亮会把当前项滚进视口。
- 开发态 5173 已是本包 Vite 时复用，不再因端口占用退出；被其他进程占用才报错。
- 全局搜索的消息命中会标出所属会话；点击打开该会话并滚到命中的那条消息，短暂高亮提示。线程回复落到主转录里的父消息。
- 聊天正文里的 `@群成员` 会渲染成带头像和名称的 chip，点击打开该 Bot 人设；`@everyone` 同样显示为 chip，但不打开人设。代码块里的 `@` 仍是纯文本。
- 预览面板不再提供下载按钮；目录和未知类型仍可用系统打开或在 Finder 显示。
- 群聊不论是否有进行中的轮，作曲栏和流式气泡都不出现 Stop。群任务发出后不能用按钮打断；要停就发消息让 Bot 们停下来。`POST /v1/turns/stop` 和托盘 Stop 会拒绝或跳过群轮。清空历史、归档、删除群仍会结束该会话的活轮。
- 同一条消息挂了多个工作区路径时，气泡里收成一个入口（点开后预览栏左侧是引用路径树），不再铺一整排文件芯片。
- 产物预览的文本改为只读 CodeMirror 查看器：行号、查找（无替换）、折叠、括号匹配、换行、复制，以及用系统打开 / 在 Finder 显示。Markdown 和单文件 HTML 默认仍看渲染结果，可切源码。聊天围栏代码块继续用 Shiki。
- 侧栏底部可打开整个工作区目录（⌘O）：左侧是工作区树，右侧是编辑器。
- 产物预览和工作区浏览的文本改为 Monaco + Shiki（vitesse 亮/暗跟随外观）。区内 UTF-8 文本可编辑，Cmd+S 或工具条保存写回同一路径；未保存切文件或关闭会确认。文件树和内容之间可拖改宽度。聊天围栏代码块继续用 Shiki。

### Landing

- 落地页整体重新设计：冷灰底、宋体标题、取自名册头像调色板的青绿 / 芥黄两色，去掉渐变与玻璃卡片，零 webfont。
- 首页改为随滚动推进的完整流程演示：右侧固定一扇信使窗口，8 个步骤依次演示填工作区与端点、建第一个 Bot、对话组队、群里参与判断、批准卡、@ 交接与产物、Monaco 预览与 Cmd+S 写回、关窗进托盘；每步配一条指向界面元素的标注。窄屏改为顶部吸附的窗口加文字说明；`prefers-reduced-motion` 下直接显示各步终态。
- 新增品牌标识：一只装着两个叠放队友头像的消息气泡（青绿 / 芥黄），提供 `favicon.svg`、PNG 图标（48 / 180 / 192 / 512）与 `site.webmanifest`；导航、页脚与重定向页统一使用。
- 补齐 SEO：每页独立的标题与描述、canonical、`hreflang` 交替链接、Open Graph 与 Twitter 大图卡片、首页 SoftwareApplication JSON-LD、`theme-color`、按语言输出的 `<html lang>`，以及预渲染的 `sitemap.xml` 与 `robots.txt`。中英文各一张 1200×630 的 OG 图（`static/og-zh.png` / `static/og-en.png`），由开发态专用的 `/og/[lang]` 页面截图生成。
- 导航栏新增外观切换（跟随系统 / 亮色 / 暗色），默认跟随系统，显式选择存在 `localStorage` 的 `real-bot-theme`，首屏脚本在绘制前应用避免闪烁。演示窗口改用信使自己的亮 / 暗两套配色令牌，批准卡、预览编辑器、托盘桌面、标注和光标都随主题切换；OG 图生成页固定亮色。
- 「已接入 / 正在建设 / 明确不做」改为按维度排列的账目式表格；「从源码启动」保留终端示例并列出首次使用步骤。中英文文案同步更新；`/manifesto` 与 `/roadmap` 页面沿用新样式，浅色与深色主题均可用。

### Documentation

- 明确本地 Grok Bot 式协作、开放模型与 MCP 接入、agent 决策与任务反馈、通过对话管理应用的核心方向。
- 增加中英文项目介绍、WIP 能力状态与路线图，将原有开发细节保留到 `docs/development.md`。
- 添加 MIT 许可证、第三方源码声明、贡献指南、行为准则、安全说明和 Issue / PR 模板。
- 补充本地规划、验证产物和敏感运行数据的忽略规则，说明首次公开前需要独立完成的检查。
- 增加 GitHub Actions：PR / `main` 测试与类型检查、落地页 Pages 部署、`v*` 标签打未签名 macOS 快照 draft。
